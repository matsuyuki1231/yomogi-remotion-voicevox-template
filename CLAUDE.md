# Remotion + VOICEVOX 動画テンプレート 詳細ガイド

ずんだもん＆めたんの掛け合い紹介動画を作成するための完全ガイドです。

---

## 目次

1. [クイックスタート](#クイックスタート)
2. [GUIエディター](#guiエディター)
3. [Claude Codeでの使い方](#claude-codeでの使い方)
4. [セリフの書き方](#セリフの書き方)
5. [英語の発音問題](#英語の発音問題)
6. [キャラクター画像](#キャラクター画像)
7. [スタイル設定（video-settings.yaml）](#スタイル設定video-settingsyaml)
8. [手動での使い方](#手動での使い方)
9. [ファイル構成](#ファイル構成)
10. [トラブルシューティング](#トラブルシューティング)
11. [Tips](#tips)

---

## クイックスタート

```bash
# 1. テンプレートをコピー
git clone https://github.com/nyanko3141592/remotion-voicevox-template.git my-video
cd my-video
npm install

# 2. VOICEVOXを起動

# 3. プレビューサーバーを起動
npm start
# → http://localhost:3000 でプレビュー確認

# 4. Claude Codeで開く（別ターミナル）
claude
```

---

## GUIエディター

スクリプトや設定をブラウザから編集できるGUIエディターを搭載しています。

### 起動方法

```bash
# 初回のみ: 依存関係のインストール
npm run editor:install

# エディターを起動
npm run editor
```

- **エディター画面**: http://localhost:3001
- **API**: http://localhost:3002
- **Remotion Studio**: http://localhost:3000（別途 `npm start` で起動）

### 機能

| タブ | 説明 |
|------|------|
| Script | セリフの一覧表示・編集・追加・削除 |
| Settings | video-settings.yaml の編集 |

### Script画面

- テーブル形式でセリフを一覧表示
- 行クリックで編集モーダルが開く
- キャラクター・表情はドロップダウンで選択
- Visual（画像/テキスト）、SE（効果音）も設定可能
- 「+ Add Line」で新規セリフ追加

### Settings画面

- フォント、字幕、キャラクター、動画、カラーの設定をフォームで編集
- 保存時に `npm run sync-settings` が自動実行される

### Claude Code連携API

Claude CodeがGUIを経由せずにスクリプトを操作するためのAPIも提供しています：

```bash
# 全メタデータ取得（token節約）
curl http://localhost:3002/api/metadata/all

# スクリプト取得
curl http://localhost:3002/api/script

# スクリプト更新
curl -X PUT http://localhost:3002/api/script/1 \
  -H "Content-Type: application/json" \
  -d '{"text": "新しいセリフなのだ！"}'
```

---

## Claude Codeでの使い方

### 基本の流れ

```
┌──────────────────────────────────────┐
│ 1. 「〇〇の紹介動画を作りたい」        │
│         ↓                           │
│ 2. Claudeがセリフを作成               │
│         ↓                           │
│ 3. 「音声生成して」                   │
│         ↓                           │
│ 4. 「プレビュー見せて」で確認          │
│         ↓                           │
│ 5. 修正があれば指示                   │
│         ↓                           │
│ 6. 「動画出力して」で完成！            │
└──────────────────────────────────────┘
```

### よく使う指示

#### 動画を作る
```
「Homebrewの紹介動画を作りたい」
「Pythonの基礎を説明する動画を作って。初心者向けに」
「このアプリの使い方動画を作りたい」
```

#### セリフを修正する
```
「ID 5のセリフを『〇〇〇』に変更して」
「シーン2のセリフをもっと短くして」
「めたんのセリフをもっと増やして」
「専門用語を減らして」
```

#### 発音を修正する
```
「GitHubをギットハブって発音して」
「英語の発音がおかしいところを全部カタカナにして」
```

#### 生成・出力する
```
「音声を生成して」
「プレビュー見せて」
「動画を出力して」
```

---

## セリフの書き方

### ファイル: `src/data/script.ts`

```typescript
export const scriptData: ScriptLine[] = [
  {
    id: 1,                              // ユニークID（連番）
    character: "zundamon",              // "zundamon" または "metan"
    text: "こんにちは！",                // 音声生成用
    displayText: "Hello!",              // 字幕用（省略可）
    scene: 1,                           // シーン番号
    voiceFile: "01_zundamon.wav",       // 音声ファイル名
    durationInFrames: 100,              // 音声生成後に自動更新
    pauseAfter: -2,                     // セリフ後の間（フレーム数）-2で固定にしてください
    emotion: "happy",                   // 表情（省略可）
  },
];
```

### キャラクターの口調

| キャラクター | 役割 | 語尾 | 性格 |
|-------------|------|------|------|
| ずんだもん | 説明役 | 「〜なのだ！」「〜のだ」 | 元気、明るい |
| めたん | 聞き役 | 「〜わ」「〜ね」「〜かしら？」 | 落ち着いた、質問上手 |

---

## 英語の発音問題

VOICEVOXは英語を正しく発音できません。`text`にカタカナ、`displayText`に英語を設定します。

```typescript
{
  text: "ホームブルーでインストールするのだ！",      // 音声用
  displayText: "Homebrewでインストールするのだ！", // 字幕用
}
```

### よく使う変換表

| 英語 | カタカナ |
|------|---------|
| macOS | マックオーエス |
| iPhone | アイフォン |
| GitHub | ギットハブ |
| API | エーピーアイ |
| AI | エーアイ |
| Homebrew | ホームブルー |
| Ctrl+S | コントロールプラスエス |
| IME | アイエムイー |

### 漢字の読み間違い

VOICEVOXは**同じ漢字の別の読み**を平気で当ててくる。`text` をひらがな／カタカナにして直す
（`displayText` や各HUDのテロップは漢字のままでよい）。

| 書き方 | 誤った読み | 直し方 |
|--------|-----------|--------|
| そちらの方は | ホ'オワ | `そちらのかたは` |
| 入り方は | イリガタワ | `はいりかたは` |
| 七人目 / 7人目 | シチニンメ | `ナナニンメ`（`ななにんめ` だと ナ'/ナ'ニン/メ' と割れる） |
| 第二話 / 第2話 | ダ'イ/ニワ'ワ と割れる | `ダイニワ`（カタカナで1句に収まる） |
| マイクラ人狼 | マイクラ'ジン/オ'オカミ | `マイクラジンロウ`（単独の「人狼」なら ジンロオ と正しく読む） |
| ため口 | タメ'コオ | `タメぐち`（口だけひらがなにする） |
| 経験の有無 | ユウム になりやすい | `けいけんのありなし` と言い換える |
| DAY 1 | アルファベットは読み飛ばされる | `デーワン`（デエワ'ン）。`デイワン` だと デ'/イ'ワン と割れる |
| 1分で〜 | `いっぷん` は イッ'/プ'ン と割れ、漢数字の `一分で` は **イチ'ブデ** と誤読される | **半角数字の `1分`**（イッ'プン）。※`一分もかからない` は イッ'プンモ で正しく、**同じ語でも文脈で読みが変わる**ので毎回確かめる |
| 統合版 | `トウゴウバン` とカタカナで書くと ト'オ/ゴオ'バン と割れる | **漢字のまま**（トオゴオ'バン） |
| ymg24.org | アルファベットは読み飛ばされる | `ワイエムジイ ニイヨン ドット オオアアルジイ`。伸ばし棒（`ワイエムジー`）だと ワイエ'ム/ジイ' と割れる。読点で区切ると1語ごとに間が空いて尺が1秒近く伸びるので**空白区切り**にする |
| 19132 | 数字は読み飛ばされる。`19132のまま` と書くと イチマンキュウセンヒャクサンジュウニ と読まれる | `イチキュウイチサンニ`。1桁ずつ空白で区切ると聞き取りやすいが尺が2.4秒伸びるので、画面に数字を大きく出して補う |

**読みは推測せず、VOICEVOXの `audio_query` で必ず確かめる。** カタカナ読みと
アクセント句の区切りがそのまま返ってくるので、区切りが不自然な表記も弾ける：

```bash
# speaker: 2=四国めたん / 3=ずんだもん
curl -s -X POST "http://127.0.0.1:50021/audio_query?speaker=2&text=$(
  python -c "import urllib.parse,sys;print(urllib.parse.quote(sys.argv[1]))" "そちらのかたは？")" \
  | python -c "import sys,json;print(json.load(sys.stdin)['kana'])"
# -> ソチラノ'/カタ'ワ？
```

スクリプトを作ったら**全行ぶんまとめて流して目視する**とよい（`config/script.yaml` の
`text:` 行を抜いてループさせる）。1行ずつ聞き直すより速く、確実に拾える。

**既知の未修正箇所**: `config/script.living-server-reply.yaml` と `config/archive/` の
複数スクリプトに「入り方」が残っており、イリガタと読まれる。また
`config/script.werewolf.yaml`（報道型・人狼版）の「マイクラ人狼イベント」は
マイクラ'ジン/オオカミイ'ベント と読まれる。これらは投稿済み動画の
文言なのであえて直していない（作り直すときに合わせて直すこと）。

---

## キャラクター画像

### フォルダ構造

```
public/images/
├── zundamon/
│   ├── mouth_open.png      # 通常・口開き（必須）
│   ├── mouth_close.png     # 通常・口閉じ（必須）
│   ├── happy_open.png      # happy表情（任意）
│   ├── happy_close.png     # （任意）
│   └── ...
└── metan/
    └── （同様）
```

**注意:** 表情差分は任意です。`npm run sync-settings`で画像フォルダをスキャンし、存在しない表情は自動的に`mouth_open/mouth_close`にフォールバックします。

### 表情の使い方

**基本ルール:**
- 基本は`normal`（口パク）で話す
- 表情差分は**多用しない**、ここぞというところで使用
- リアクションは最低0.5秒（15フレーム）継続させる

**使いどころ:**
| 表情 | 使うタイミング |
|------|----------------|
| `normal` | 通常の説明、会話（デフォルト） |
| `happy` | 嬉しいとき、褒めるとき、ポイント強調 |
| `surprised` | 驚いたとき、意外な事実 |
| `thinking` | 考え込むとき、説明を聞くとき |
| `sad` | 残念なとき、問題点を指摘 |

```typescript
// NG: 表情を多用しすぎ
{ text: "すごいのだ！", emotion: "happy" },
{ text: "便利なのだ！", emotion: "happy" },

// OK: ここぞというところで使う
{ text: "すごいのだ！" },  // normal（省略可）
{ text: "これが一番のポイントなのだ！", emotion: "happy" },  // ← ここぞ
```

### 画像パスの変更

`video-settings.yaml`で設定：

```yaml
character:
  useImages: true               # 画像を使用する
  imagesBasePath: "images"      # public/images/{characterId}/
  # または共有フォルダ
  # imagesBasePath: "/Users/shared/characters"
```

### 画像の入手先

| キャラクター | 入手先 |
|-------------|--------|
| ずんだもん | [公式](https://zunko.jp/con_illust.html)、ニコニ・コモンズ |
| 四国めたん | [公式](https://zunko.jp/con_illust.html)、ニコニ・コモンズ |

※ 各素材の利用規約を必ず確認してください

---

## スタイル設定（video-settings.yaml）

### デフォルト（黒板風デザイン）

```yaml
# フォント設定
font:
  family: "M PLUS Rounded 1c"   # ポップ体
  size: 70
  weight: "900"                 # エクストラボールド
  color: "#ffffff"              # 白文字
  outlineColor: "character"     # キャラクターごとの色

# キャラクター設定
character:
  height: 275
  useImages: true
  imagesBasePath: "images"

# カラー設定（黒板風）
colors:
  background: "#ffffff"
  zundamon: "#228B22"           # フォレストグリーン
  metan: "#FF1493"              # ディープピンク
```

### おすすめフォント

| フォント | 特徴 |
|----------|------|
| M PLUS Rounded 1c | 丸ゴシック、ポップ（デフォルト） |
| Noto Sans JP | 標準的、読みやすい |
| Kosugi Maru | 丸ゴシック、親しみやすい |

---

## 手動での使い方

### コマンド一覧

| コマンド | 説明 |
|---------|------|
| `npm start` | プレビュー（http://localhost:3000） |
| `npm run voices` | 音声生成（VOICEVOX起動必須） |
| `npm run build` | 動画出力（out/video.mp4） |
| `npm run init` | 新規プロジェクト初期化（スクリプトをリセット） |
| `npm run sync-settings` | YAML設定を反映＋画像スキャン |
| `npm run editor` | GUIエディター起動（http://localhost:3001） |
| `npm run editor:install` | GUIエディターの依存関係インストール |

### 手順

1. `src/data/script.ts` を編集
2. `npm run voices` で音声生成
3. `npm start` でプレビュー確認
4. `npm run build` で動画出力

---

## ファイル構成

```
├── video-settings.yaml      # ★ スタイル設定
├── src/
│   ├── data/
│   │   └── script.ts        # ★ セリフデータ
│   ├── components/
│   │   ├── Character.tsx    # キャラクター表示
│   │   ├── Subtitle.tsx     # 字幕
│   │   └── SceneVisuals.tsx # シーン別ビジュアル
│   ├── config.ts            # 基本設定
│   └── Main.tsx             # メインコンポーネント
├── editor/                  # GUIエディター
│   ├── src/                 # フロントエンド（React + Vite）
│   └── server/              # バックエンド（Express）
├── public/
│   ├── images/              # キャラクター画像
│   └── voices/              # 音声ファイル（自動生成）
└── out/
    └── video.mp4            # 出力動画
```

---

## トラブルシューティング

### VOICEVOXに接続できない
```
Error: ECONNREFUSED
```
→ VOICEVOXアプリが起動しているか確認

### 音声と字幕がずれる
→ `npm run voices` を再実行（durationInFramesが自動修正される）

### 英語の発音がおかしい
→ `text`をカタカナに、`displayText`に英語を設定

### キャラクター画像が表示されない
→ `video-settings.yaml`の`useImages: true`を確認
→ 画像パスを確認

### 音声ファイルが見つからない
```
Error: Could not find file: voices/XX_zundamon.wav
```
→ `npm run voices` で音声を生成

---

## コンテンツ表示（画像・テキスト）

### 画像を表示

セリフごとに`visual`フィールドで画像を指定：

```typescript
{
  id: 3,
  character: "zundamon",
  text: "これがインストール画面なのだ！",
  visual: {
    type: "image",
    src: "install-screen.png",  // public/content/内の画像
    animation: "fadeIn",
  },
}
```

### テキストを表示

```typescript
{
  visual: {
    type: "text",
    text: "ポイント1",
    fontSize: 72,
    color: "#ffffff",
    animation: "bounce",
  },
}
```

### アニメーション

| animation | 効果 |
|-----------|------|
| `none` | アニメーションなし |
| `fadeIn` | フェードイン（デフォルト） |
| `slideUp` | 下から上へスライド |
| `slideLeft` | 右から左へスライド |
| `zoomIn` | 拡大しながら表示 |
| `bounce` | 弾むように表示 |

---

## BGM・効果音

### BGM設定

`src/data/script.ts`でBGMを設定：

```typescript
export const bgmConfig: BGMConfig = {
  src: "background.mp3",  // public/bgm/内のファイル
  volume: 0.3,
  loop: true,
};
```

### 効果音

セリフごとに`se`フィールドで効果音を指定：

```typescript
{
  id: 5,
  character: "zundamon",
  text: "ポイントはここなのだ！",
  se: {
    src: "chime.mp3",  // public/se/内のファイル
    volume: 0.8,
  },
}
```

### フォルダ構造

```
public/
├── bgm/           # 背景音楽
├── se/            # 効果音
├── content/       # コンテンツ画像
├── images/        # キャラクター画像
└── voices/        # 音声ファイル（自動生成）
```

---

## Tips

### 二人で「バイバイ」を合わせる

```typescript
{
  id: 44,
  character: "metan",
  text: "バイバイ〜！",
  pauseAfter: 0,  // ← 間を0に
},
{
  id: 45,
  character: "zundamon",
  text: "バイバイなのだ〜！",
  pauseAfter: 60,
},
```

### 解説に必要な素材を指示する

スクリプトにコメントで記載：

```typescript
{
  id: 5,
  text: "こんな感じでインストールするのだ！",
  // <<ターミナルでbrew installを実行しているスクリーンショット>>
},
```

### コンテンツ表示のルール

- コンテンツは**画面全体を使って最大限大きく**表示
- 無駄な余白は作らない
- 字幕とキャラクターはコンテンツの上に重ねて表示

### よもぎサーバーの解説動画について

よもぎサーバーについての動画の作成を指示された場合、以下の点を遵守していただくようお願いします

 - よもぎサーバーの情報はdocs/yomogi配下にあります。これ以外の情報は参照しないでください
 - よもぎサーバーに入ったことの無い人に対して宣伝することを目的としていますので、情報を事細かに伝えるよりかは、キャッチーな文言となるようにしてください
 - 全部の長所を網羅的に伝えるのではなく、特定の長所をピックアップして紹介する形にしても構いません
 - 動画の中に「ネットで『よもぎサーバー』と検索すると詳細を確認できる」旨と「よもぎサーバーに是非参加してほしい」旨を入れてください。ただし、動画の流れに合わせて文意を損なわない程度に使用する語句を替えていただく分には全く問題ありません
 - 可能な限り画像や動画を使用してください。よもぎサーバーの動画はpublic/content配下にあります

### よもぎサーバー動画のスタイル設定

よもぎサーバーの動画は以下のスタイルで統一してください。

#### 映像・レイアウト

| 設定 | 値 |
|------|----|
| アスペクト比 | 9:16（ショート動画、1080×1920） |
| 尺 | **45〜75秒**。完了率を保てる限りは情報密度の高い長めが有利（下記の設計原則1を参照）。認定試験型（77秒）・相場クイズ型（78秒）のように1カット1情報を最後まで保てる構成なら**80秒手前まで**は許容している。ただしこれは上限であって目標ではない。長くなったら**まず全行のセリフを削る**（体言止め・修飾を落とす）こと。情報そのもの（問題数・解説の行数）を削るのは最後の手段 |
| 背景 | 全編に実映像（マイクラ素材）。素材がない行だけ `MillionBackdrop` / `RewindBackdrop` / `RespawnBackdrop` / `InterviewBackdrop` / `ReplyBackdrop` / `CourtBackdrop` / `NewsBackdrop` のCSS背景。**求人票型だけは例外で、前半は実映像をあえて1枚も出さない**（下記参照） |
| キャラクター立ち絵 | **使わない** |

#### ショート動画として守る設計原則

フォーマットを作り替えるときも、ここは崩さないこと。伸びるショートの条件として
一貫して挙がる項目で、いまのフォーマットはこれを構造として満たすように組んである。

1. **完了率がすべて。そのうえで情報密度で尺を稼ぐ**。かつては15〜30秒が有利とされたが、
   2026年時点ではShortsの平均視聴時間が伸び、**45〜75秒で情報密度の高い動画**が
   優遇される方向に変わっている。短くするより「3秒ごとに新しい情報を出し続ける」ことを優先する
   （＝1カット1情報。間延びした瞬間に離脱するので、密度が保てないなら短くする）
2. **冒頭1秒は「起」ではなく「転」**。前置きゼロで、いちばん強い一撃をそのまま1行目に置く
   （断言・煽りコメント・クライマックスの絵）。3秒フックではなく1秒フックで考える
3. **同じ絵を5秒以上続けない**。カット・テロップ・BGMのどれかを数秒ごとに変える
4. **末尾を冒頭につなげてループさせる**。2025年3月末からループ再生も再生回数に
   加算されるため、最終行の映像は1行目と同じ `src` / `startFrom` にする
5. **結論を言い終えたあとに余白を作らない**。最後の行の `pauseAfter` は 0
6. **音を出さずに見られている前提**。デカ文字テロップを必ず出す
7. **コメントを誘発する「隙」を企画段階で設計する**。ツッコミどころ・異論の余地・
   問いかけのいずれかを構造に埋め込み、最終行で明示的に開く（「質問、まだある？」など）
8. **常設の"あと何"メーターを1本置く**。有罪の心証・値札・未回答件数のように、
   「これがゼロになったら何が起きるのか」で最後まで引っぱる装置を必ず1つ用意する。
   ただし**ドラマ仕立ての型では、メーターが結末の予告になってはいけない**
   （「逆転まであと○秒」を出すと作り話だと分かってしまう）。その場合は
   縦型ショートドラマ型のように「積み上がって差が開いていくだけ」の装置に置き換える
9. **非言語の演出で伝わることを、言語で説明しない**（フォーマットを問わず適用する）。
   色・音・間・カット・アニメーションで既に伝わっていることに文字を重ねない。
   - カラーグレードやUIの色が反転して空気が変わったのに「立場、逆転」と実況しない
   - これから起きる転換を「逆転まで 00:34」「この後、○○します」と先に予告しない
   - シークバーやタイムラインに転換点のマーカーを打たない
   - 白フラッシュ・効果音・BGM切替で足りている瞬間に、さらにテロップを足さない
   - 画面のテロップと同じ内容を字幕でも読ませない（`hidesSubtitle` の役割）

   **理由**: 物語の外側から画面が実況すると、作り手の枠組みが露出して
   「胡散臭い作り話」に見える。視聴者が自分で気づく余地を奪った時点で体験が壊れる。
   テロップ・字幕・常設UIを1つ足すたびに「これは色・音・間・カットで既に伝わって
   いないか？」を先に確認し、伝わっているなら足さない。効果音も「ドン」と決めず、
   場面に必要な最小限の質にとどめる。

   **例外**: 最初から茶番だと分かっている型（裁判の有罪の心証メーター、通販の値札、
   コメント欄の未回答件数、取材のドット列）は、パロディUIそのものが企画なので
   文字で説明・予告してよい。判断基準は「視聴者にリアルだと信じてほしいか否か」。

10. **最下部の流れるティッカー帯を作らない**（2026年8月5日〜。今後の新フォーマット全部）。
    既存14フォーマットはどれも最下部92pxに右→左へ流れる要約文の帯を置いていたが、
    **今後作るフォーマットではこの帯そのものを設けない**（`*Ticker` フィールドも
    定義しない）。既存フォーマットのコードは残してあるが、流用して新しい型を
    作るときはティッカーを外すこと。

    **理由**: 字幕・テロップ・カード・常設メーターで情報はすでに出ており、
    帯の文字は誰も追って読まない。9番の「伝わっていることに文字を重ねない」の
    実例で、画面下92pxを恒久的に潰すぶん映像の見える面積が減るほうが損。

    ティッカーを置かないぶん、字幕は下へ寄せて余白を詰めてよい。ただし
    **`bottomOffset` は `video-settings.yaml` のグローバル設定で、フォーマット別に
    持てない**（既存14型はティッカー帯の上へ逃がすために `150` を要求する）。
    既存型が現役のあいだは `150` のままにして、新フォーマット側は
    「字幕を出す行を検索CTAだけに絞る／その行では下部のUIを畳む」で対応する
    （認定試験型はこの方法をとっている）。

#### スクリプトとフォーマットの対応

| ファイル | 中身 | HUD |
|----------|------|-----|
| `config/script.living-server-lie2.yaml` | **生活サーバー版・ウソ発見器・ウソ当て型 第2弾（最新／75秒29行）** | `LieHud` |
| `config/script.werewolf-lie.yaml` | マイクラ人狼版・ウソ発見器・ウソ当て型（71秒29行） | `LieHud` |
| `config/script.living-server-lie.yaml` | 生活サーバー版・ウソ発見器・ウソ当て型（79秒31行） | `LieHud` |
| `config/script.living-server-market.yaml` | 生活サーバー版・相場クイズ・値札当て型（78秒33行） | `MarketHud` |
| `config/script.living-server-exam.yaml` | 生活サーバー版・認定試験・答案採点型（77秒33行） | `ExamHud` |
| `config/script.living-server-rail.yaml` | 生活サーバー版・路線図・車内アナウンス型（60秒30行） | `RailHud` |
| `config/script.werewolf-join.yaml` | マイクラ人狼版・参加導線ハウツー型（25行・Discord連携） | `JoinHud` |
| `config/script.living-server-join.yaml` | 生活サーバー版・参加導線ハウツー型（56秒29行） | `JoinHud` |
| `config/script.living-server-promo.yaml` | 生活サーバー版・正直CM・王道PR型（45秒26行） | `PromoHud` |
| `config/script.living-server-millionaire.yaml` | 生活サーバー版・クイズ$ミリオネア型（54秒26行） | `MillionHud` |
| `config/script.werewolf-screenquiz.yaml` | マイクラ人狼版・画面当てクイズ型（21行） | `QuizHud` |
| `config/script.living-server-screenquiz.yaml` | 生活サーバー版・画面当てクイズ型（45秒25行） | `QuizHud` |
| `config/script.living-server-rewind.yaml` | 生活サーバー版・巻き戻し型（51秒29行） | `RewindHud` |
| `config/script.living-server-respawn.yaml` | 生活サーバー版・リスポーン型 | `RespawnHud` |
| `config/script.living-server-job.yaml` | 生活サーバー版・求人票/募集要項型 | `JobHud` |
| `config/script.living-server-drama.yaml` | 生活サーバー版・縦型ショートドラマ/逆転劇型 | `DramaHud` |
| `config/script.werewolf-drama.yaml` | **マイクラ人狼版・縦型ショートドラマ型（疑心暗鬼と吊り間違い）** | `DramaHud` |
| `config/script.living-server-interview.yaml` | 生活サーバー版・街頭インタビュー/突撃取材型 | `InterviewHud` |
| `config/script.living-server-reply.yaml` | 生活サーバー版・コメント返信/反論処理型 | `ReplyHud` |
| `config/script.living-server-shop.yaml` | 生活サーバー版・テレビショッピング/通販型 | `ShopHud` |
| `config/script.living-server-court.yaml` | 生活サーバー版・裁判/尋問型 | `CourtHud` |
| `config/script.living-server-news.yaml` | 生活サーバー版・緊急速報/報道型（謎の巨大都市） | `NewsHud` |
| `config/script.werewolf.yaml` | マイクラ人狼版・緊急速報/報道型（住民が消える村） | `NewsHud` |
| `config/script.yaml` | **ビルド対象**。上のどれかをコピーして使う | — |

`Main.tsx` は **スクリプトのフィールド接頭辞**でフォーマットを自動判定し
（`lie*` → ウソ発見器・ウソ当て型、`mkt*` → 相場クイズ・値札当て型、
`exam*` → 認定試験・答案採点型、`rail*` → 路線図・車内アナウンス型、
`join*` → 参加導線ハウツー型、`pv*` → 正直CM・王道PR型、`mil*` → クイズ$ミリオネア型、
`quiz*` → 画面当てクイズ型、`rw*` → 巻き戻し型、`resp*` → リスポーン型、
`job*` → 求人票型、`drama*` → 縦型ショートドラマ型、`intv*` → 街頭インタビュー型、
`reply*` → コメント返信型、`shop*` → 通販型、
`court*` → 裁判型、どれでもなければ報道型）、
`LieHud` / `MarketHud` / `ExamHud` / `RailHud` / `JoinHud` / `PromoHud` / `MillionHud` / `QuizHud` /
`RewindHud` / `RespawnHud` / `JobHud` /
`DramaHud` / `InterviewHud` / `ReplyHud` / `ShopHud` / `CourtHud` / `NewsHud`（背景・暗幕・常設UI・
字幕抑止まで含めて）を切り替える。
**`join*` と `job*` は互いに素**（j-o-i と j-o-b）なので接頭辞の判定順に依存しない。
**`rail*` も既存のどれとも互いに素**（r で始まるのは `rw*` / `resp*` / `reply*`）。
**`exam*` も互いに素**（e で始まる接頭辞はほかにない）。
**`mkt*` も互いに素**（m で始まるのは `mil*` だけ）。
**`lie*` も互いに素**（l で始まる接頭辞はほかにない）。**`config/archive/` の旧クイズ型2つも `quiz*` を使うが
フィールド名が違うので現行の `QuizHud` では描画できない**（復元はタグから）。
スクリプトをコピーするだけで見た目が丸ごと入れ替わるので、コード側の切り替えは不要。
分岐は `Main.tsx` の `renderBackdrop` / `renderScrim` / `renderChrome` / `renderHud` の
4関数にまとまっているので、フォーマットを増やすときはここに1ケース足すだけでよい。

**スクリプトYAMLでは `text` を `character` の直後に置くこと。**
`generate-voices.ts` の行パーサは正規表現で `id` → `character` → …→ `text` → …→ `voiceFile`
の順に拾う。以前は `character` の**直後**が `text` であることを要求していたため、
あいだに別フィールド（`joinTone` など）を挟んだ行が**黙って音声生成から漏れ**、
前の版の wav が残ったまま尺だけ既定値（60フレーム）になる事故が起きた。
現在はあいだのフィールドを許容し、さらに**パース件数が合わなければ即エラーで落とす**
ようにしてあるが、順序は慣習として守ること。

ビルド手順: `cp config/script.<版>.yaml config/script.yaml` → `npm run sync-script` →
`npm run voices` → `npm run build`。**版を切り替えたら必ず `npm run sync-script` を先に実行してから
`npm run voices` をやり直す**（`generate-voices.ts` は `src/data/script.ts` を読むので、
sync を飛ばすと前の版のセリフで音声を作ってしまう。音声ファイル名 `01_metan.wav` … は
版をまたいで共通なので、前の版の音声が残っていると尺がズレる）。`概要欄.txt` も版ごとに差し替える。

過去のフォーマットは `config/archive/` に文言のみ残してあり、対応コンポーネントは削除済み
（復元する場合は `format-*-v1` タグを参照。怪しい勧誘・詐欺疑い型＝
`config/archive/script.living-server-scam.yaml` / `format-scam-v1`、3択クイズ型＝
`config/archive/script.living-server-quiz3.yaml` / `format-quiz3-v1`）。
**街頭インタビュー型（`InterviewHud`）・コメント返信型（`ReplyHud`）・通販型（`ShopHud`）・
裁判型（`CourtHud`）・報道型（`NewsHud`）は削除していない** —
直近の生活サーバー版と人狼版がこれらを使っているため。

#### 現在のフォーマット: ウソ発見器・ウソ当て型（よもぎ生活鯖 ウソ発見器）

**捨て札をやめる**。既存のクイズ型4つ（画面当てクイズ型／クイズ$ミリオネア型／
認定試験・答案採点型／相場クイズ・値札当て型）に共通していたのは **1問＝1事実** という
構造で、選択肢のうち正解以外の2つは捨て札（ありえない数字・ありえない機能）だった。
3枚出しているのに紹介できるのは1件だけ、という取りこぼしが常にあった。

この型はそこを反転させる。**3枚のうち1枚だけがウソで、残り2枚は本当**。捨て札が
消えるので **1問で2件** 紹介でき、さらに解説パネルでウソを否定するときに3件目の事実が
出る。全8問で **本当のことが24件** 乗る。紹介できる密度は既存27型で最大。

**この型も「ユニークさより、紹介の質」を優先して作った**（2026年8月5日のユーザー指示、
認定試験型・相場クイズ型に続く3本目）。

**芯は「ウソくさいほうが、本当だった」**。ウソ札は「8マスまで」「審査がいる」
「非公開」のような *いかにもありそうな制限* にしてあり、逆に「上下50m動ける」
「運営が月3万YGくれる」のような *ありえない話* が本当。数問で
「ありえないほうを信じればいい」と学習させ、その学習そのものを「この鯖、そこまで
やってるのか」という宣伝に変える。**最終問題（参加費・スマホ・24時間）だけはルールを
裏切って、ウソが1枚もない**。ウソを混ぜてきた側が最後だけ混ぜなかった、という形で
宣伝そのものを保証する。

画面の構造：上＝実映像、下＝**供述カード3枚**（縦積み）。出題中は下端でポリグラフ
（嘘発見器の波形）が走り続け、解答するとウソ札が赤く染まって「ウソ」の判が押され、
**斜めに裂けて落ちる**。残った本当の札は上へ詰めて「本当」の判が押され、
その下が解説パネルになる。値札（`MarketHud`）が吊り下げ物で画面を割ったのに対し、
こちらは**積まれた紙**で割る。

進行：id1「マイクラで、上下50メートル、動けるのだ」（＝ありえない話を言い切る）→
めたん「そんなの、ウソに決まってるわ」→「本当なのだ。ここからウソを7つ混ぜるのだ」
→ **この3行だけでゲームのルールが説明ゼロで伝わる** → 全8問（移動／採掘／商売／会社／
治安／土地／ガチャ／参加）→ id20 で **残るウソが 0** → めたん「ウソ、ぜんぶ見つけたわ」
→ **なのに第8問が始まる**（この型いちばんの仕掛け）→「ウソは、ないのだ」→
**全画面の事実リスト24件（2列×12行）** →「ぜんぶ、公式ドキュメントに書いてあるのだ」→
めたん「ゲームの中に、社会があるのね」→ まとめ帯 → 検索CTA →
「あなたは、いくつ見破れた？」でループ。

**いちばん濃いのは第5問（治安）**。処罰が点数制で、5点で永久Ban、4点以下は1か月ごとに
1点消え、しかも**生活鯖・マイクラ人狼・Discordで処罰が独立している**。運営の作り込みが
いちばん伝わるので、この問だけセリフを削らない。

**この型は「説明してよい」型**。ウソ発見器のパロディUIなので、残件数も事実リストも
画面が明示するのが本体。

**ウソ札は解答行で必ず明示的に否定する**。否定しないまま流すと、それ自体が誤情報として
視聴者に残る。解説パネルの見出しを「ウソの反対の事実」にすることで自動的に否定になる。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| ウソメーター（`lieLeft`） | **この型の"あと何"メーター**。7→0。ウソのランプが1つずつ撃ち落とされる。**0になった直後に第8問が来る**のが仕掛けの入口 |
| 供述カード3枚（`lieCards`） | 縦積み。記号（ア・イ・ウ）を振ってあるのでコメントで答えられる |
| ポリグラフ（出題行のみ） | 波形が走り続け、下段に制限時間バーと「ウソは、どれ？」。答えを考える時間を持たせる装置 |
| カードの裂け（`lieShowAnswer`） | ウソ札に「ウソ」の判が押されてから斜めに裂けて落ち、本当の札が上へ詰めて「本当」が押される |
| 解説パネル（`lieExplain`） | 見出し＋補足2行＋**出典（docsのページ名）**。認定試験型・相場クイズ型と同じく**この型の本体** |
| 事実リスト（`lieList`） | 全画面2列×12行の調書。**この型のクライマックスでスクショ対象** |

`LieChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。残件数は Main が「前の行の値 → いまの行の値」と現在行の
開始フレームを渡し、消えた1灯だけ弾けさせる。

**供述カードは「出題が始まってから、事実リストが出るまで」しか出さない**
（`lieBoardShown()`）。導入の3行と事実リスト以降は畳んで映像を全画面に開く。

**ツッコミだけの行では、直前のカードと解説を残す**（`lieCardResolved`）。
持ち越しの行は `held` を立ててアニメーションを焼き直さない。

**解説パネルはカードの落下より16フレーム遅らせて開く**（`held` の行を除く）。
同時に出すと落下中のカードに覆いかぶさって画が濁る。

**解説パネルの縦位置はカードの枚数で決まる**（ウソが落ちて2枚なら 1404、
最終問題のように3枚残るなら 1546）。3枚残る行では補足を2行までに抑えること。

**映像は `SceneVisuals` の `lie` モードで上部エリア（204〜1004）にだけ収める**
（相場クイズ型の `market` と同じエリア・同じ定数を共用している）。**エリアの位置を
変えるときは `SceneVisuals` の `MARKET_TOP` / `MARKET_HEIGHT` も一緒に直すこと**。

**供述カードは15文字以内**（1行に収める）。テーマプレートのラベルは4文字前後、
解説の見出しは2行まで、補足は2行まで、事実リストの項目は11文字以内。

**生活サーバー版は2本ある**。1本目（`script.living-server-lie.yaml`）が
エレベーター／役職の採掘単価／チェストショップと有人レジ／会社の設立と公認企業／
処罰の点数制／土地の売買／ガチャ／参加費、2本目（`script.living-server-lie2.yaml`）が
エフェクトの秒課金／最低販売価格の適用範囲／遠隔購入と「検索機能をわざと作らなかった」
設計思想／島の拡張と返金なし／称号の作成制限と色分け／チェスト・ドア保護／
優良企業ガイドラインの労働条件。**最終問題（参加費・スマホ・24時間）だけが共通**で、
残り7問は完全に別。**2本目でいちばん濃いのは第3問**で、機能の有無ではなく
**運営の設計思想**（スポーン近くの土地の価値を守るため検索機能を作らなかった）が出る。

この型は**まだ動画にしていない要素を8問ぶん束ねれば何本でも作れる**。
3本目を作るときは `lieFacts` を1・2本目とdiffして重複を確認すること
（最終問題の3件だけは意図的に共通）。

**人狼版（`config/script.werewolf-lie.yaml`）は同じHUD・同じフィールドのまま
題材だけ差し替えてある**（29行・71秒）。**この型と人狼の相性がとくに良い**のは、
マイクラ人狼が**ルールの説明量が多い**ゲームだから。ふつうの紹介動画だと
「毎週土曜21:30にやってます」で終わって中身が1つも伝わらないが、この型なら
**ルールそのものをクイズの本体にできる**ので、遊び方を教えることと宣伝が同じ動作になる。
さらに人狼は**「ウソをつくゲーム」そのもの**なので枠が題材と噛み合い、第3問（CO）の
答えが「COでウソをつける」になる——ウソを見破らせる動画で「このゲームはウソをついていい」
と教えることになるのがこの版のいちばん効いている点。
出題は 武器／会議／CO／役職／タスク／サボタージュ／死んだあと／参加 の8問。

**人狼で実在する動画素材は4本だけ**なので、生活サーバー版の31行に対して**29行に抑え**、
同じカットの再利用は必ず8行以上あける。コンタクトシートで確認した使える区間は
`会議中の風景`（360f）が 30/90/150/210、`会議中の風景2`（456f）が 30/90/150/250
（**f≈210 と f≈390 は黒い人物がカメラを塞ぐ**）、`殺し屋が霊媒師のフリ`（450f）が
30/90/150（**f≈270 以降はチャット欄が全面に開く**）、`霊媒師で市民勝利`（450f）が
150/210/270/320（**f≈120 以前はチャット欄**）。

**出題行のセリフは長めに書く（＝シンキングタイム）**。この型は**セリフが終わると
画面が切り替わる**ので、考える時間は出題行の音声の長さでしか作れない。初版は
1問あたり2.5〜3.3秒しかなく、3枚を読み終わる前に答えが出てしまっていた。
現行は**問番号＋テーマ＋ひとこと（30文字前後）**にして4.7〜6.4秒に伸ばしてある
（画面上は playbackRate 1.5 で 3.2〜4.3秒）。ひとことは毎回変えて、
「ありえないほうが本当かもしれない」というこの型のルールを言い換え続けるとよい。
**間（`pauseAfter`）を正の値にして黙らせる方法は取らない**（設計原則5の余白ゼロと、
セリフで持たせるという方針から外れる）。

**読みの注意**：問番号は「第2問」〜「第6問」「第8問」を**カタカナ**
（`ダイニモン` / `ダイサンモン` / `ダイヨンモン` / `ダイゴモン` / `ダイロクモン` /
`ダイハチモン`）、「第1問」「第7問」を**ひらがな**（`だいいちもん` / `だいななもん`）に
すると1句に収まる。カタカナの `ダイナナモン` は ダ'イ/ナナモン' と割れる。
「18コ」「24コ」は ジュウ'/ハチ'コ と割れるので**カタカナ**（`ニジュウヨンコ`）。
「24時間」は ニ'ジュウ/ヨジ'カン と割れるので**セリフでは読ませず、画面のカードにだけ出す**。
「感圧板」は カ'ン/アツ'/バ'ン と3句に割れるので**ひらがな（`かんあつばん`）**。

**`生活ワールドの街並みを散策している動画.mp4` の `startFrom: 2560` はレンダリングで
落ちる**（`No frame found at position`。still では通るが本番のレンダリングで失敗する）。
この素材で実績があるのは **1000 / 1080 / 1160 / 1350 / 2600 / 2700 / 2780 / 2860**。
新しい値を使うときは、still ではなく**本番のレンダリングで通ることを確認する**。

#### スクリプトのフィールド（lie*）

| フィールド | 用途 |
|-----------|------|
| `lieTone` | `test`（測定中・シアン）/ `clear`（鑑定終了・金・カードが畳まれる）。**指定した行から後ろに引き継がれる** |
| `lieTitle` | 番組名。**最初に指定した行のものを動画全体で使う** |
| `lieNo` | 何問目か。**指定がない行は直前の値を引き継ぐ**。分母は最大値。**この値がない行ではカードを出さない** |
| `lieLeft` | まだ見破っていないウソの件数。**指定がない行は直前の値を引き継ぐ**。減った行だけ演出が入る |
| `lieHook` / `lieHookSub` | 冒頭の大テロップ。改行は `\n` で**明示する** |
| `lieTheme` / `lieThemeLabel` | テーマプレートの本文とラベル（移動 / 採掘 / 会社 など） |
| `lieCards` | 供述カード（3枚想定）。**出題行と解答行の両方に同じ内容を書く**。**15文字以内** |
| `lieAnswer` | ウソの位置（**0始まり**）。位置は毎回散らす。**`-1` ならウソなし**（最終問題） |
| `lieTimer` | 出題行に `true`。ポリグラフの制限時間バーがセリフの尺いっぱいで縮む |
| `lieShowAnswer` | 解答行に `true`。ウソ札が裂けて落ち、残りに「本当」が押される |
| `lieExplain` / `lieExplainSub` / `lieSource` | 解説パネルの見出し・補足（`\n` で改行）・出典（docs のページ名） |
| `lieFacts` | **事実リストに載せる項目**（解答行に書く。配列）。**11文字以内** |
| `lieRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `lieFlash` / `lieFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `lieList` / `lieListSub` | 事実リスト（全画面・白フラッシュ）。ここでトーンが金に反転する。**24件を読ませるには2行にまたがって書く**（2行目は完成形から始まる） |
| `lieReveal` / `lieRevealSub` | まとめ帯（正式名称と条件を大きく出す） |
| `lieCta` / `lieNote` | 検索バー風CTA と、その下の小さな但し書き（**記載の時点を明示する**） |
| `lieResult` / `lieResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 0〜112 / ウソメーター 124〜196 / **実映像 204〜1004** /
テーマプレート 1024〜1108 / **供述カード（出題）1128〜1610** /
ポリグラフ 1638〜1838（出題行だけ）/ **供述カード（解答）1128〜1376（2枚）or 1518（3枚）** /
解説パネル 1404（2枚）or 1546（3枚）〜 / テロップ 500〜 / ツッコミ吹き出し 772〜 /
まとめ帯 760〜 / ループリボン 790〜 / CTA 1156〜 / 注記 1300〜。事実リストだけは全画面。

#### 併存フォーマット: 相場クイズ・値札当て型（よもぎ生活鯖 相場クイズ）

**当てさせるものを、値段だけに固定する**。既存のクイズ型3つ（画面当てクイズ型／
クイズ$ミリオネア型／認定試験・答案採点型）はどれも**当てさせ方**の設計で、
何を当てさせるかは毎回ばらばらだった。よもぎ生活鯖は「生活・経済サーバー」で、
いちばん作り込まれているのは経済のほうなのに、既存27型のどれも価格を正面から
扱っていない。1問1価格に絞ると、**下限価格規制・手数料・エフェクトの秒課金・
露天掘り代行の期待値**まで、ふつうの紹介動画には絶対に入らない粒度の情報が
10個そのまま乗る。

**この型も「ユニークさより、紹介の質」を優先して作った**（2026年8月5日のユーザー指示、
認定試験型に続く2本目）。数字は具体的であるほど「そこまで決まってるのか」に化けるので、
**驚きと宣伝が同じ動作になる**のがこの型の効率のよさ。

**芯は「見終わったとき、あなたの手元に、この鯖の価格表が1枚残る」**。
当たった／外れたの気持ちよさより、**最後に出る表がスクショに値すること**を優先している。
そのため価格表は最後にいきなり出すのではなく、出題中ずっと画面下半分で
**1行ずつ育っていくところを見せる**（記帳ずみプレビュー）。前に出た値段の再露出にもなる。

画面の構造：上＝実映像（その値段のものが動いている画）、下＝**市場の値札**。
3枚の値札が横棒から紐でぶら下がって揺れており、解答すると**不正解の2枚は紐が切れて
回りながら落ち**、正解の1枚だけが残って「確定」の刻印が押される。答案用紙（`ExamHud`）が
紙で画面を割ったのに対し、こちらは**吊り下げ物**で割る。

進行：id1「マイクラの中に、値段のルールがあるのだ」＋大テロップ「全10問／相場、当てられる？」→
めたん「値段のルール？ ゲームでしょ？」→ **id3「ダイヤを安く売ったら、規約違反なのだ」**
（いちばん強い一撃）→ 全10問（土地100YG／石2YG／ダイヤの最低価格2300YG／店の手数料7%／
スピード上昇2YG毎秒／称号5000YG／ガチャ1200YG／**露天掘り代行32050YG**／島300万YG／参加費0円）→
**全画面の価格表（2行ぶん出しっぱなし）** →「10個ぜんぶ、公式のドキュメントに書いてある値段なのだ」→
めたん「ゲームの中に、経済があるのね」→ まとめ帯 → 検索CTA →「あなたは、何問当たった？」でループ。

**いちばん濃い1問は第8問（露天掘り代行）**。賞金でも商品でもなく、**運営が期待値を計算して
投票で決めた価格**（1チャンクに平均ダイヤ3.5個と鉄80個 → 2300×3.5＋300×80＝32050YG）。
「ここまでやってる鯖なのか」がいちばん伝わるので、この問だけセリフを削らない。

**この型は「説明してよい」型**。相場クイズのパロディUIなので、記帳件数も価格表も
画面が明示するのが本体。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| 記帳メーター（`mktFilled`） | **この型の"あと何"メーター**。0→10件。相場表の10行ぶんのセグメントが最初から並んでいて、1問答えるごとに1つ光って埋まる |
| 吊り下げ値札3枚（`mktChoices`） | 横棒から紐でぶら下がって**それぞれ違う位相で揺れる**。記号（ア・イ・ウ）を振ってあるのでコメントで答えられる |
| 値札の落下（`mktShowAnswer`） | 不正解の2枚の紐が切れて回りながら落ち、正解が横長1枚に畳まれて「確定」が押される |
| 解説パネル（`mktExplain`） | 見出し＋補足2行＋**出典（docsのページ名）**。認定試験型と同じく**この型の本体** |
| 記帳ずみプレビュー | 出題中の下半分。直近4件を出し、空きは点線の罫線で残す。表が育つ過程と残りが同時に見える |
| 価格表（`mktTable`） | 全画面の台帳。**この型のクライマックスでスクショ対象** |

`MarketChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。記帳件数は Main が「前の行の値 → いまの行の値」と現在行の
開始フレームを渡し、増えた1件だけ光らせる。

**売り場（値札の横棒）は「出題が始まってから、価格表が出るまで」しか出さない**
（`mktBoardShown()`）。導入の3行と価格表以降は畳んで映像を全画面に開く。

**ツッコミだけの行では、直前の商品と値札と解説を残す**（`mktCardResolved`）。
持ち越しの行は `held` を立ててアニメーションを焼き直さない。

**解説パネルは値札の落下より14フレーム遅らせて開く**（`held` の行を除く）。
同時に出すと落下中の値札に覆いかぶさって画が濁る。

**映像は `SceneVisuals` の `market` モードで上部エリア（204〜1004）にだけ収める**
（`exam` モードと同じ考え方。定数だけ別）。**エリアの位置を変えるときは
`SceneVisuals` の `MARKET_TOP` / `MARKET_HEIGHT` も一緒に直すこと**。

**商品プレートは13文字以内、値札は7文字以内**（3枚横並びで1枚328px）。
解説の見出しは2行まで、補足は2行まで。

**読みの注意**：「第6問」は ダ'イロ/ク'モン、「第8問」は **ダ'イワ/チ'モン**（＝ダイワチモン）
と誤読されるので**カタカナで `ダイロクモン` / `ダイハチモン`** と書く。ほかの問番号は
ひらがな（`だいいちもん` など）で1句に収まる。金額は「YG」が読み飛ばされるので
**半角数字＋ワイジイ**（`5000ワイジイ` → ゴセンワイ'ジイ）。ただし `32050ワイジイ` は
サンマ'ン/ニセ'ン/ゴジュウワイ'ジイ と3句に割れるので**`サンマンニセンゴジュウワイジイ`**
とカタカナで書く。`1200ワイジイ` は セ'ン/ニヒャクワイ'ジイ と2句になるが自然なので許容。

**素材の終端に寄せすぎない**。`生活ワールドの街並みを散策している動画.mp4` は
`video-durations.json` に 3565 とあるが、実体は 118.84秒 / 29.91fps ＝ **3554フレーム**しかなく、
`startFrom: 3200` でレンダリングが「No frame found at position」で落ちた。
この素材は **2900 以下**に収めること。

#### スクリプトのフィールド（mkt*）

| フィールド | 用途 |
|-----------|------|
| `mktTone` | `deal`（取引中・琥珀）/ `settled`（記帳ずみ・緑・売り場が畳まれる）。**指定した行から後ろに引き継がれる** |
| `mktTitle` | クイズ名。**最初に指定した行のものを動画全体で使う** |
| `mktNo` | 何問目か。**指定がない行は直前の値を引き継ぐ**。分母は最大値。**この値がない行では売り場を出さない** |
| `mktFilled` | 相場表に記帳ずみの件数。**指定がない行は直前の値を引き継ぐ**。増えた行だけ弾む |
| `mktHook` / `mktHookSub` | 冒頭の大テロップ。改行は `\n` で**明示する** |
| `mktItem` / `mktItemLabel` | 商品プレートの本文（何の値段を当てるのか）とラベル（土地 / 採掘 / 店 など）。**13文字以内** |
| `mktChoices` | 値札（3枚想定）。**出題行と解答行の両方に同じ内容を書く**。**7文字以内** |
| `mktAnswer` | 正解の位置（**0始まり**）。位置は毎回散らす |
| `mktTimer` | 出題行に `true`。制限時間バーがセリフの尺いっぱいで縮む |
| `mktShowAnswer` | 解答行に `true`。不正解の値札が落ち、正解に「確定」が押される |
| `mktExplain` / `mktExplainSub` / `mktSource` | 解説パネルの見出し・補足（`\n` で改行）・出典（docs のページ名） |
| `mktRowLabel` / `mktRowValue` | **価格表に載せる1行**（解答行に書く）。ラベルは12文字以内、値段は短く |
| `mktRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `mktFlash` / `mktFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `mktTable` / `mktTableSub` | 価格表（全画面・白フラッシュ）。ここでトーンが緑に反転する。**10行を読ませるには2行にまたがって書く**（2行目は完成形から始まる） |
| `mktReveal` / `mktRevealSub` | まとめ帯（正式名称と条件を大きく出す） |
| `mktCta` / `mktNote` | 検索バー風CTA と、その下の小さな但し書き（**価格の時点を明示する**） |
| `mktResult` / `mktResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 0〜112 / 記帳メーター 124〜196 / **実映像 204〜1004** /
商品プレート 1024〜1136 / 制限時間バー 1150 / 値札を吊るす横棒 1176 /
値札3枚 1214〜1450 / **記帳ずみプレビュー 1490〜1880**（出題行だけ）/
確定した値札 1214〜1364 / **解説パネル 1396〜1880**（解答行だけ）/
テロップ 520〜 / ツッコミ吹き出し 772〜 / まとめ帯 760〜 / ループリボン 790〜 /
CTA 1156〜 / 注記 1300〜。価格表だけは全画面。

#### 併存フォーマット: 認定試験・答案採点型（よもぎ生活鯖 認定試験）

**クイズはつかみ、解説が本体**。既存のクイズ型2つ（画面当てクイズ型／クイズ$ミリオネア型）は
どちらもクイズが主役で、機能の説明は正解の根拠1行だけだった。紹介できる要素が
「1問＝1フレーズ」に圧縮されるので、よもぎ生活鯖のいちばん濃い部分（会社制度・下限価格規制・
役職の単価）がどうしても入りきらない。この型はそこを反転させる。1問ごとに答案用紙の下半分が
丸ごと**解説パネル**に変わり、見出し＋補足2行＋**出典（docsのページ名）**まで出す。

**この型は「ユニークさより、紹介の質」を優先して作った**（2026年8月5日のユーザー指示）。
構造の新規性で勝負するのではなく、10問＝10機能を出典つきで確実に伝えることを最上位に置く。

画面の構造：上半分＝実映像（機能が動いている画）、下半分＝**答案用紙**（クリーム色の紙・
罫線・綴じ穴・赤ペン）。オーバーレイが映像の上に浮いている既存14型と違い、
**紙が物理的に画面を上下に割る**。正解すると赤ペンで丸が描かれる
（SVGの `strokeDasharray` で実際に筆記される）。

進行：id1「よもぎ生活サーバー、認定試験なのだ！」＋大テロップ「全10問／あなたは何点？」で
**前置きゼロで試験が始まり、1行目で視聴者が受験者にされる** → めたん「え、私が受けるの？」→
「7問正解で、合格なのだ」→ 全10問（役職の単価／最低販売価格／土地／チェストショップ／
会社／公認企業／釣り275種／島／近距離VC／参加費0円）→ **全問正解で100点の認定証**
（全画面・白フラッシュ＋朱印。ここでトーンが藍→金に反転し、答案用紙が畳まれる）→
めたん「これ、全部本当にあるの？」→「ぜんぶ、公式のドキュメントに書いてあるのだ」→
まとめ帯 → 検索CTA →「あなたは、何点だった？」でループ。

**芯は「全問正解できたら、あなたはもう住人です」**。解けた人は"知っている人"として
気持ちよく、解けなかった人には**知らない機能がまだあること自体が宣伝になる**。
どちらに転んでも刺さるので、当てさせるか外させるかで悩まなくてよい。

**この型は「説明してよい」型**。検定のパロディUIなので、得点も合格ラインも画面が明示するのが本体。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| 得点メーター（`examScore`） | **この型の"あと何"メーター**。0→100点。加点した行だけ数字が弾んで「+10」が浮き上がる |
| 合格ラインのマーカー（`examPass`） | バーの70%位置に立つ。Q7（釣り275種）で越えてメーターが金に変わる |
| 答案用紙 | 下半分を占める紙。1カット1問で設問と選択肢が入れ替わる |
| 赤ペンの丸（`examShowAnswer`） | 解答行で選択肢を1つに畳んで、右端に赤い丸が**描かれる**。採点されている画 |
| 解説パネル（`examExplain`） | 紙の下半分。見出し＋補足2行＋出典。**この型の本体** |
| 認定証（`examCert`） | 全画面の賞状＋朱印。試験の終わり＝宣伝への転換点 |

`ExamChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。得点は Main が「前の行の値 → いまの行の値」と現在行の開始フレームを
渡し、増えた瞬間だけ弾ませる。

**答案用紙は「出題が始まってから、合格するまで」しか出さない**（`examPaperShown()`）。
導入の3行と認定証以降は紙を畳んで映像を全画面に開く。ここを常設にすると、
1行目のフックで下半分が白紙になって間延びする。

**ツッコミだけの行では、直前の設問と解説を紙に残す**（`examCardResolved`）。
空の紙が出ないうえ、解説を読む時間が伸びる＝この型の狙いに合う。
持ち越しの行は `held` を立ててアニメーションを焼き直さない。

**映像は `SceneVisuals` の `exam` モードで上部エリア（206〜1080）にだけ収める**
（画面当てクイズ型の `screen`、路線図型の `rail` に続く3例目の映像側改変）。
いつもの 160%×125%（実質4倍以上）で敷くと、プラグインのGUIも風景もドアップになって
何が映っているか分からなくなる。エリアの高さに合わせて縦125%に収め、上下だけを逃がす
（＝座標表示とホットバーは切れる）。**紙がない行では全画面に戻す**ので、
下半分が黒く空くことはない。

**素材は「解説の内容が映っているカット」を選ぶ**。この型は解説パネルと映像が
同じ機能を指しているほど強い（役職＝`roleコマンドで役職を変更している動画` の f255＝
役職一覧が読める位置、会社＝`会社の社員一覧や売上履歴を見ている動画` の f1000＝帳簿のGUI、
原木＝`人工資源で原木を掘っている動画` の f160）。`exam` モードは拡大が控えめなので、
**GUI素材がそのまま読める**のがこの型の利点。

**設問文は14文字以内**（1行に収める。2行に折ると選択肢に食い込む）。
選択肢は10文字以内、解説の見出しは2行まで、補足は2行まで。

#### スクリプトのフィールド（exam*）

| フィールド | 用途 |
|-----------|------|
| `examTone` | `test`（試験中・藍）/ `pass`（合格・金・答案用紙が畳まれる）。**指定した行から後ろに引き継がれる** |
| `examTitle` / `examExaminee` | 試験名と受験者名。**最初に指定した行のものを動画全体で使う** |
| `examPass` | 合格ライン（点）。得点メーターにマーカーが立つ |
| `examNo` | 何問目か。**指定がない行は直前の値を引き継ぐ**。分母は最大値。**この値がない行では紙を出さない** |
| `examScore` | 得点。**指定がない行は直前の値を引き継ぐ**。増えた行だけ弾んで「+10」が飛ぶ |
| `examHook` / `examHookSub` | 冒頭の大テロップ。改行は `\n` で**明示する** |
| `examQ` | 設問文。**14文字以内**。書いた行から後続に引き継がれる |
| `examChoices` | 選択肢（3つ想定）。**出題行と解答行の両方に同じ内容を書く** |
| `examAnswer` | 正解の位置（**0始まり**）。位置は毎回散らす |
| `examTimer` | 出題行に `true`。制限時間バーがセリフの尺いっぱいで縮む |
| `examShowAnswer` | 解答行に `true`。選択肢を畳んで正解だけ残し、赤ペンで丸が描かれる |
| `examExplain` / `examExplainSub` / `examSource` | 解説パネルの見出し・補足（`\n` で改行）・出典（docs のページ名） |
| `examRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `examFlash` / `examFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `examCert` / `examCertSub` | 認定証（全画面・白フラッシュ＋朱印）。ここでトーンが金に反転する |
| `examReveal` / `examRevealSub` | まとめ帯（正式名称と条件を大きく出す） |
| `examCta` / `examNote` | 検索バー風CTA と、その下の小さな但し書き |
| `examResult` / `examResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 0〜118 / 得点メーター 130〜206 / **実映像 206〜1080** /
テロップ 520〜 / ツッコミ吹き出し 846〜 / **答案用紙 1080〜1880** /
　設問 1124〜 / 選択肢 1272〜1642 / 解答＋解説 1272〜1840 /
まとめ帯 760〜 / ループリボン 790〜 / CTA 1156〜 / 注記 1300〜。
認定証だけは全画面。

**映像エリアの位置を変えるときは `SceneVisuals` の `EXAM_TOP` / `EXAM_HEIGHT` も
一緒に直すこと**（HUDと映像は別ファイルにある）。

#### 併存フォーマット: 路線図・車内アナウンス型（よもぎ生活線）

**視聴者を乗り物に乗せて移動させる**。既存27型はすべて「1枚の画面に、カード／プレート／
チップ／メーターが出入りする」構造だった。報道も裁判も通販もクイズも求人票もドラマも、
視聴者はずっと同じ場所に座って、目の前を情報が通り過ぎるのを見ていた。この型はそこを壊す。

画面の上半分は**電車の車内案内表示器（LED）**で、路線名・「次は○○」・**横向きの路線図**・
運賃が常設で出る。中央には**駅名標**（白い盤面・駅ナンバリング・ローマ字・前後の駅）が
右から流れてきて減速停止する＝電車が駅に着く動き。走行中は窓の外＝実映像が高速で横に流れて
車体が揺れ、停車すると止まる。

**この型の芯は「全部の駅で降りなくていい。1駅でいい」**（id20〜22）。既存の型はどれも
「できることを盛る」方向だったが、盛ったうえで**ひとつ選べと言う**。未参加層の
「入っても何をすればいいか分からない」「ついていけなさそう」に正面から答える。
参加導線型が「入り方が面倒くさそう」に答えたのと対になる位置づけ。

進行：id1「次は、あなたの家。あなたの家なのだ」（＝持っていない人の家が次の駅として
announce される違和感がそのままフック）→ めたん「私、家なんて持ってないわよ」→
「この電車に乗れば、持てるのだ」→「駅の名前、全部おかしいわよ」→ 発車 →
土地前／大工町／商店街／会社前／資源ヶ丘／釣り堀／車庫前／ガチャ広場／声の丘／島 の
10駅に停車（1駅1機能）→ めたん「全部の駅で、降りなきゃダメなの？」→ **「1駅でいい」** →
終点 あなたの家 →「で、運賃は？」→ **0円スラム**（トーンが蓬緑→金に反転）→ まとめ帯 →
検索CTA →「この路線、環状線なのだ／終点は始発です」→「あなたは、どの駅で降りる？」でループ。

**環状線という設定が、ループ再生を構造的に正当化する唯一の型**。最終行の映像と「次は」表示を
id1 と同じに戻すので、継ぎ目が完全に閉じる。

**この型は「説明してよい」型**。車内案内表示器のパロディUIなので、残り駅数や運賃を画面が
明示するのが本体（ドラマ型とは逆）。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| 横向きの路線図（`railStops`） | **この型の"あと何"メーター**。全11駅が常に見えていて、電車アイコンが左から右へ進む。通過ずみのラインが路線カラーで塗られる |
| 「終点まで あと○駅」 | 走行中は向かっている駅も残りに数え、終点に停まったら「終点」に変わる |
| 運賃（`railFare`） | 「？？？円」のまま終点まで引っぱり、id26 で 0円 に確定して金に反転する |
| 駅名標（`railSign`） | 1カット1駅。**右から流れてきて減速停止する**動きがカットの句読点になる |
| 走行／停車（`railMoving`） | 走行中は映像が高速で横に流れて車体が揺れ、停車すると止まる |

`RailChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。電車の位置は Main が「前の行の駅番号 → いまの行の駅番号」を渡し、
バネで1区間ずつ滑らせる。

**走行感は `SceneVisuals` の `rail` モードが担う**（画面当てクイズ型の `screen` に続く
2例目の映像側改変）。`moving` では横パンの周期を 130 フレームまで詰めて景色を流し、
`stopped` では 1600 フレームまで伸ばしてほぼ止める。**この型だけはパンの向きを
右→左に固定する**（lineId の偶奇で反転させると進行方向が変わってしまう）。
加えて `railShake()` で車体の縦揺れを足す。

**素材の当て方**：`生活ワールドの街並みを散策している動画.mp4` は **f600〜750 と
f1800〜2400 でチャット欄・コマンド画面が開いていて使えない**。使えるのは f900〜1650 と
f2550〜3500。`新しい土地を土地保護している動画.mp4` は全編が夜＋チャットログで暗いので
この型では使わない（土地は明るい街並みで代用し、次の駅の建築中の木の家と対比させる）。
会社の帳簿GUI（`会社の社員一覧や売上履歴を見ている動画.mp4` の f≈1000）は暗い文字だらけなので
2カット続けない。近距離VCと島の素材は存在しないので、人がいる街並み／作り込まれた街の
俯瞰で補う。

**駅名の読み**：「〜前」の駅名は VOICEVOX で必ず2句に割れる（トチ／マエ）。
**カタカナで書くとアクセントまで崩れる**（`シャコマエ` → シャ'/コ'マエ）ので、
`土地前` `車庫前` のように**漢字のまま書く**（シャ'コ/マ'エ と正しく区切られる）。
逆に `大工町`（ダイクチョウと読まれうる）と `資源ヶ丘` はカタカナのほうが安全。

#### スクリプトのフィールド（rail*）

| フィールド | 用途 |
|-----------|------|
| `railTone` | `ride`（乗車中・蓬緑）/ `arrive`（終点・金）。**指定した行から後ろに引き継がれる** |
| `railLine` / `railDest` | 路線名（よもぎ生活線）と行き先（あなたの家）。**最初に指定した行のものを動画全体で使う** |
| `railStops` | 全駅の駅名（配列）。路線図はこれで描く。**最初に指定した行のものを全体で使う** |
| `railTicker` | 最下部のLEDティッカーを流れる1文。全行ぶんが連結されて常時流れる |
| `railNo` | いま何駅目か（1始まり）。**指定がない行は直前の値を引き継ぐ**。走行行には「これから向かう駅」の番号を書く |
| `railMoving` | **引き継がない**。`false` を書いた行だけ停車（映像が止まり、電車が駅にぴたりと着く） |
| `railNext` | 「次は」に出す自由文。駅番号がある行では路線図の駅名が優先されるので、効くのは発車前の導入部だけ |
| `railFare` | 常設の運賃表示（？？？円 → 0円）。**指定がない行は直前の値を引き継ぐ**。「？」を含まなくなると金に反転 |
| `railSign` / `railSignSub` / `railSignCode` | 駅名標の駅名・ローマ字・駅ナンバリング（YG-04） |
| `railSignPrev` / `railSignNext` | 駅名標の下段に出す前後の駅 |
| `railInfo` / `railInfoLabel` / `railInfoSub` | 駅の説明プレート本文・ラベル（土地 / 会社 など）・補足行 |
| `railRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `railFlash` / `railFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `railFareSlam` / `railFareSlamSub` | 運賃0円スラム（全画面・白フラッシュ＋金の集中線） |
| `railReveal` / `railRevealSub` | まとめ帯（正式名称と条件を大きく出す） |
| `railCta` / `railNote` | 検索バー風CTA と、その下の小さな但し書き |
| `railResult` / `railResultSub` | ループ用リボン（「環状線」バッジつき。コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
LEDヘッダ（路線名・行き先・運賃）0〜110 / 「次は○○」表示 126〜204 /
**横向きの路線図 224〜434**（`ROUTE_TOP`。駅名は縦書き）/ **駅名標 476〜810** /
テロップ 620〜 / まとめ帯 760〜 / ループリボン 790〜 / ツッコミ吹き出し 940〜 /
説明プレート・CTA 1128〜 / 注記 1284〜 / LEDティッカー 最下部92px。
運賃0円スラムだけは全画面。

**駅名標（476）とテロップ（620）は重なる**ので、`railSign` を書いた行には `railFlash` を
書かない。

#### 併存フォーマット: 参加導線ハウツー型（入り方、これだけ）

**CTAそのものを動画の本体にする**。既存26型はすべて「魅力を見せる → 最後の1行で
『よもぎサーバーで検索』」で終わっていた。CTAは動画のオマケで、実際に参加するかどうかは
視聴者が動画を閉じたあとの話だった。この型はそこを壊す。前半は**参加手順の実演**
（STEP 1〜5）、後半は**入った直後にやること**の案内で、見ながらそのまま参加できる。

未参加層の最大の障壁は「面白そうだけど、入り方が面倒くさそう」。そこに正面から答える。
手順を最後まで見せること自体が「本当に誰でも入れる」の証明になるので宣伝の信憑性が上がり、
後半の機能紹介も「入ったあと何をすればいいか」という実用情報の形で出るので押し売りにならない。

進行：id1「入り方、これだけなのだ！」で**結論（もう入れた画）から始める**（この行だけ
トーンが `inside` なので映像がぼけない）→ めたん「もう入ってるじゃない」→ id3 で
トーンが `setup` に落ちて映像がぼけ、**経過時間カウンターが回り始める** →
STEP 1 統合版を開く／STEP 2 サーバーのタブ／STEP 3 サーバーを追加／
STEP 4 アドレス `ymg24.org`・ポート `19132`／STEP 5 追加してプレイ →
id12「入れたのだ！」で**白フラッシュ＋トーン反転＋ぼけが取れる** →
入ったあとにやること（役職→採掘→土地→家→店→会社→車→釣り→VC）が
チップとして積み上がる → 参加費0円スラム → まとめ帯 →
Switchはフレンド追加（`ymg24mc`）→ 検索CTA →「入ったら、まずどこ行く？」でループ。

**画面の主役はマイクラ統合版の「サーバーを追加」画面をCSSで再現したパネル**。
スクショを貼るのではなく描画するので、`ymg24.org` が実際にタイプされ、ポートが入り、
緑の「追加してプレイ」が押し込まれるところまで動く。`docs/yomogi/living/img/how_to_join_*.png`
に実物のスクショがあるが、**縦1080幅では文字が小さすぎるうえ注釈が焼き込まれている**ので使わない。

**転換（id12）は文字で説明しない**。`JoinScrim` が `setup` のあいだ映像に
`backdropFilter: blur(12px)` をかけて沈め（＝マイクラのメニュー画面の背景に見える）、
`inside` でぼけが取れて実映像がクリアになる。「入った」をピントで伝えるのがこの型の芯。

**この型は「説明してよい」型**。パロディでもドラマでもなく実用ハウツーなので、
常設メーター（STEPバーと経過時間）で残りを明示してよいどころか、明示が本体。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| STEPバー（`joinStep`） | 前半の"あと何"メーター。1→5。進んだ瞬間だけ弾み、進行中の1本が脈打つ |
| 経過時間カウンター | **演出ではなく動画の実経過時間**。`joinClockStart` の行から回り `joinClockStop` の行で止まり、以降は確定値として出しっぱなしになる。「1分で入れる」と言ったものが実際は **00:18** で終わるので期待を上回る（id13「本当に1分もかかってないわ」で回収） |
| やったことチップ（`joinGot`） | 後半の"あと何"メーター。役職→採掘→土地→家→店→会社→車→釣り→VC の9個 |
| マイクラUIパネル（`joinScreen`） | 1カット1操作。`joinFocus` の対象に白枠とタップ波紋が出る |

`JoinChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。パネルの画面（`joinScreen`）とフォームの入力値
（`joinName` / `joinAddress` / `joinPort`）は Main が引き継いで解決し、
`inside` に入った時点でパネルを捨てる（実映像の上に白いパネルを残さない）。

**素材の当て方**：前半は暗幕が濃い（`rgba(6,14,26,0.62)` ＋ blur）ので何を敷いても
暗くなる。効くのは id1／id12／id29 の3枚で、**id1 と id29 は同じ明るい昼のカット**
（ループの継ぎ目）、**id12 はその素材でいちばん明るいカット**にすること。
後半は1カット1機能なので、機能と内容が一致する素材を当てる
（役職＝`roleコマンドで役職を変更している動画` の f200 前後＝役職一覧が出ている位置、
店＝`チェストショップでオーブを購入している動画`、車＝`生活サーバーで車に乗っている動画` の
f60 前後＝明るくて車体が写る位置）。
**近距離VCの素材は存在しない**ので、人がいる街並みを当てて補う。

**人狼版（`config/script.werewolf-join.yaml`）は同じHUD・同じフィールドのまま
題材だけ差し替えてある**（25行）。**決定的な違いは手順そのものが別物**で、
生活サーバーが「アドレスを入れて入るだけ」なのに対し**人狼はDiscord連携が必須**。
そのぶん「面倒くさそう」という障壁が高いので、手順を最後まで実演して見せる
この型との相性はむしろ人狼のほうが良い。パネルに映す画面も `discord`（チャンネル一覧＋
チャットログ＋入力欄）と `code`（連携コード）を新設して対応した。進行は
STEP 1 Discordに入る／STEP 2 `#おやくそく-rules` で ✅／STEP 3 `#bot操作-command` で
「1! new ゲーマータグ」／STEP 4 マイクラで `ymgs.f5.si` につないで8桁の連携コードを確認／
STEP 5 「1! auth コード」→ 連携完了 → 当日は土曜21:30に `#マイクラ人狼VC` に入るだけ →
会議／PvP／役職47種類／初参加OK／参加費0円。

**人狼のサーバーアドレスは `ymgs.f5.si`**。docs内で記述が矛盾しており
`wolf/how-to-join/join-with-address.md` の本文④だけが `ymg24.org` になっているが、
**同じページの要約を含む5箇所が `ymgs.f5.si` で一致している**ので本文④を生活版からの
コピペ漏れと判断した（ユーザー確認ずみ）。役職数は intro.md の41種類ではなく、
日付つきで新しい all_roles.md の47種類。**近距離VCはBasic人狼では使わない**ので謳わない。

**人狼で実在する動画素材は4本だけ**なので、生活サーバー版の29行に対して**25行に抑えて**
使い回しの目立ちを減らしてある。前半（setup）は暗幕＋blurで沈むので目立たない。
いちばん明るいのは「殺し屋が霊媒師のフリ」の f24〜213（一人称視点の人狼HUD＝役職表示が
右に映る）で、連携完了スラムと役職の行に当てた。

#### スクリプトのフィールド（join*）

| フィールド | 用途 |
|-----------|------|
| `joinTone` | `setup`（手順中・青・映像がぼける）/ `inside`（入ったあと・蓬緑・ピントが合う）。**指定した行から後ろに引き継がれる** |
| `joinTitle` / `joinTag` | ヘッダ帯のサービス名と、ティッカー左の短いラベル。**最初に指定した行のものを動画全体で使う**（版を変えたらここを直す） |
| `joinTicker` | 最下部を流れる1文。全行ぶんが連結されて常時流れる |
| `joinStep` | 何ステップ目か（1→5）。**指定がない行は直前の値を引き継ぐ**。STEPバーの分母は最大値 |
| `joinGot` | 入ったあとに積み上がる「やったこと」チップ |
| `joinClockStart` / `joinClockStop` | 経過時間カウンターを回し始める行／止める行 |
| `joinScreen` | パネルに映す画面（`play` / `servers` / `form` / `discord` / `code`）。**指定した行から後ろに引き継がれ、`inside` で捨てられる**。`discord` / `code` はマイクラ人狼版の連携手順用 |
| `joinFocus` | ハイライトする操作対象（`play` / `tab` / `add` / `name` / `address` / `port` / `submit` / `rules` / `command` / `vc`） |
| `joinName` / `joinAddress` / `joinPort` | フォームの入力値。**指定した行から後ろに引き継がれる** |
| `joinChannel` / `joinCommand` / `joinReply` / `joinCode` | Discord画面のチャンネル名・入力欄に打つコマンド・表示するメッセージ（1行目＝BOT / 2行目＝自分。改行で区切る）・連携コード画面の8桁。**人狼版のみ** |
| `joinTyping` | この行でタイプされるフィールド（`address` など） |
| `joinPressed` | 「追加してプレイ」が押し込まれる行 |
| `joinCard` / `joinCardLabel` / `joinCardSub` | 手順カード／ヒントカードの本文・ラベル（STEP 1 / アドレス / 役職 など）・補足行 |
| `joinRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `joinFlash` / `joinFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `joinDone` / `joinDoneSub` | 参加完了スラム（全画面・白フラッシュ）。ここでトーンが反転する |
| `joinPrice` / `joinPriceSub` | 参加費0円スラム（全画面・金の集中線） |
| `joinReveal` / `joinRevealSub` | まとめ帯（正式名称と条件を大きく出す） |
| `joinCta` / `joinNote` | 検索バー風CTA と、その下の小さな但し書き |
| `joinResult` / `joinResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯（サーバー名＋経過時間）0〜118 / STEPバー or チップ列 140〜214 /
**マイクラUIパネル 248〜1128**（`PANEL_TOP` / `PANEL_HEIGHT`）/ テロップ 620〜 /
まとめ帯 760〜 / ループリボン 790〜 / 手順カード・ツッコミ吹き出し・CTA 1180〜 /
注記 1348〜 / ティッカー 最下部92px。参加完了スラムと参加費0円スラムだけは全画面。

**テロップ（620）はパネルと重なる**ので、`joinFlash` を出す行には `joinScreen` を書かない
（パネルが出る前の id1・id3 に置く）。

#### 併存フォーマット: 正直CM・王道PR型（これは宣伝です）

**フリそのものをやめる**。既存24型はすべて「何かのフリ」だった——報道・裁判・通販・
クイズ番組・求人サイト・ドラマ——正体やサーバー名を隠して好奇心ギャップで引っぱるのが
共通の骨格だった。この型は1行目で**「これは、宣伝なのだ！」と宣言し、最後の1秒まで
一切ひねらずに宣伝だけをする**。広告は普通、広告であることを隠したがる。
堂々と名乗ること自体が、この型の唯一の「転」。

ひねった型は「宣伝だと気づかせない」ことに構造コストを払っている。この型はその
コストをゼロにして、浮いたぶんを情報密度に全振りする。「宣伝です」と最初に
言い切ってしまえば、以降のどの機能紹介も押し売りに見えない（見ている側は宣伝だと
承知の上で見ているから）。

進行：id1「これは、宣伝なのだ！」（1秒フック）→ めたん「隠す気、ゼロなのね」→
「よもぎサーバーの生活鯖、できることをぜんぶ見せるのだ」→ **カタログ15連発**
（土地・家・無人店・チェスト保護・会社・釣り275種・農業・資源ワールド・バフ・車・
ガチャ・称号・島・近距離VC・参加費0円。1行1機能、3〜4件ごとにめたんのツッコミ）→
15個目の「参加費 0円」だけ全画面スラム（トーンが蓬緑→金に反転）→ まとめ帯 →
「スマホでもOK」→ 検索CTA →「参加、待ってる／どれから始める？ コメントで」で
冒頭へループ。

**この型は「説明してよい」型の極北**。最初から宣伝だと名乗っている茶番ですらない
広告なので、常設メーター（できることカウンター n/15 とヘッダの明滅する「宣伝」バッジ）
で残りを明示してよいどころか、明示そのものが企画。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| できることカウンター（`pvNo`） | **この型の"あと何"メーター**。1→15。カタログの目次そのものがメーターになる |
| 機能カード（`pvCard`） | 1カット1機能。番号バッジつきで下から滑り込む |
| ヘッダの「宣伝」バッジ | 常時明滅。正直さを画面の隅で言い続ける |
| 参加費0円スラム（`pvPrice`） | 15個目だけ全画面（金の集中線＋白フラッシュ）。カタログの最後の1行が値段 |

`PromoChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。カウンターは Main が「前の行の値 → いまの行の値」と現在行の
開始フレームを渡し、増えた瞬間だけ数字とセグメントを弾ませる。

**`PromoScrim` は全フォーマットでいちばん薄い**。素材そのものが商品カタログなので、
隠すものが何もない。機能は**すべて docs/yomogi で裏が取れるものだけ**にする。
Switch/PS系は「特殊な操作が必要・自己責任」と docs（faq/device.md）にあるので謳わず、
デバイスの話は「スマホでも遊べる」だけに留める。

#### スクリプトのフィールド（pv*）

| フィールド | 用途 |
|-----------|------|
| `pvTone` | `pitch`（宣伝中・蓬緑）/ `close`（締め・金）。**指定した行から後ろに引き継がれる** |
| `pvTicker` | 最下部を流れる1文。全行ぶんが連結されて常時流れる |
| `pvNo` | できることの番号（1→15）。**指定がない行は直前の値を引き継ぐ**。カウンターの分母は最大値 |
| `pvCard` / `pvCardLabel` / `pvCardSub` | 機能カードの本文・短いラベル（土地 / 店 など）・補足行 |
| `pvRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `pvFlash` / `pvFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `pvPrice` / `pvPriceSub` | 参加費0円スラム（全画面・金の集中線＋白フラッシュ） |
| `pvReveal` / `pvRevealSub` | まとめ帯（正式名称と条件を大きく出す） |
| `pvCta` / `pvNote` | 検索バー風CTA と、その下の小さな但し書き |
| `pvResult` / `pvResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 0〜118 / できることカウンター 140〜210 / テロップ 620〜 / まとめ帯 760〜 /
ループリボン 790〜 / ツッコミ吹き出し 940〜 / 機能カード 1120〜 / CTA 1120〜 /
注記 1276〜 / ティッカー 最下部92px。参加費0円スラムだけは全画面。

機能カード・吹き出しは `layoutLines()` が扱う。1行で収まらないときだけ句読点で2行に割り、
区切り記号がない文は折らずにフォントを縮める。

#### 併存フォーマット: クイズ$ミリオネア型（賞金ラダー＋ライフライン）

**賭かっているものを作る**。これまでのクイズ型3つ（3択＋カウントダウンリング／○×／
画面当て）は、どれも**1問ごとに完結していて失うものが無かった**。当たっても外れても
次の問題が来るだけ。この型はそこを壊す。**賞金ラダーを1段ずつ上っていく**構造にして
「いま何段目か」「あと何段で頂上か」を右端の縦一列で常設で見せ、さらに
**ライフラインという第2のリソース**（50:50 / オーディエンス / テレホン）を持たせる。
使えば×が打たれて二度と戻らない。**リソース管理は既存22フォーマットに一度も無かった装置**。

賞金ラダーは本家では画面右の縦一列で、9:16はそれをそのまま置ける唯一の比率。
6段が常に見えているので"あと何"メーターとして最も強く働く。巻き戻し型のレールが
「上から下へ降りる」のに対し、こちらは**下から上へ登る**。

進行：id1「ここは、よもぎサーバーの生活鯖なのだ」で名乗り（**この型もサーバー名を
伏せない**）、id2 めたんの「今日の挑戦者は、あなたよ」で視聴者を当事者にする。
以降は1問＝出題行＋解答行の2行で6問（釣り275種／ダイヤ鉱石20YG／島300万YG／
社員は制限なし／最低時給5,000YG／参加費0円）。Q4 で 50:50、Q5 でオーディエンス、
最終問題でテレホンを使い、ライフラインを3つとも使い切る。

**この型の芯は「賞金が架空じゃない」**。最終問題の正解「0円」で最高額 300万YG を
獲得するが、賞金は出ない。そのかわり **300万YG はこの鯖で実際に稼げる額**
（島の作成費がちょうど 3,000,000YG）。「茶番の賞金が、実在の通貨だった」に着地して、
クイズ番組の茶番と経済鯖の宣伝が最後の数行で同じものになる。宣伝が押し売りに
ならないのは、めたんが自分から「それ、本当にもらえるの？」と聞くのに答える形だから。

**芯・その2は「毎回そこまで具体的なのか、で驚かせる」**。画面当てクイズ型の芯は
「いちばんありえない選択肢が正解」だったが、こちらは**全部が具体的な数字**で、
最大値が正解とは限らない（Q2 のダイヤは 100YG ではなく 20YG）。当てずっぽうが効かない
ぶん「そこまで細かく決まってるのか」という驚きがそのまま宣伝になる。
出典は `milFact` に必ず書く（2026年に強い「意外な事実型」は出典明記で信憑性を担保する）。

**この型は「説明してよい」型**。最初からクイズ番組のパロディUIだと分かる茶番なので、
常設メーター（賞金ラダーとライフライン）で残りを明示してよい。
**賞金額とオーディエンスの投票率は演出**なので `milNote` で明示する。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| 賞金ラダー（`milStep` / `milWon`） | **この型の"あと何"メーター**。下から上へ登り、獲得した段だけ弾んで点灯する |
| ライフライン3つ（`milLifeline`） | 第2のリソース。使うと赤い×が打たれて戻らない。残数が常に見える |
| 挑戦者プレート「挑戦者 あなた」 | 視聴者が当事者だと常設で言い続ける（`milChallenger`） |
| 制限時間バー（`milTimer`） | 出題行の尺いっぱいで縮む＝答える強制力 |
| 4択 A/B/C/D（`milChoices`） | 本家と同じ2×2。オーディエンスの棒がボタンの中を左から満たす |
| ファイナルアンサー？帯（`milFinal`） | 最終問題だけ。設問カードの真上に出る |
| 賞金獲得スラム（`milWin`） | 全画面・金の集中線＋白フラッシュ。ここから茶番が現実の話に折り返す |

`MillionChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。賞金ラダーは Main が「前の行の獲得段数 → いまの行の獲得段数」と
現在行の開始フレームを渡し、増えた1段だけを弾ませる。ライフラインの使用ずみ状態も
Main の `milUsedResolved` が累積で解決する（一度使ったら後続の全行で×が残る）。

**設問カードは下端を選択肢のすぐ上に固定して、上へ伸ばす**（`QUESTION_BOTTOM`）。
上端固定にすると2行の設問でカードが下へ伸びて選択肢に食い込む。
ファイナルアンサー帯も同じコンテナに入れてあるので、設問が2行でも重ならない。
それでも**設問文は13文字以内**にすると1行で大きく出せる。

**この型では検索スクショ（`type: image` + `backgroundSrc`）を使わない**。
賞金ラダーが右上を常に占めるので、画像カードを重ねるとラダーの下に潜って読めなくなる。
CTAバー自体が検索窓なので、CTA行も `type: video` にする。

素材は**明るい昼の街並みを優先**する。この型の暗幕はスタジオを模して四隅を強く落とすので、
屋内（チェストショップ・商店街）の素材を敷くと画が潰れる。特にリビール行（id22）は
この動画でいちばん明るい画にすること。

#### スクリプトのフィールド（mil*）

| フィールド | 用途 |
|-----------|------|
| `milTone` | `quiz`（挑戦中・青）/ `win`（獲得後・緑）。**指定した行から後ろに引き継がれる** |
| `milTitle` / `milChallenger` | 番組タイトルと挑戦者名。**最初に指定した行のものを動画全体で使う** |
| `milPrizes` | 賞金ラダーの金額（下から上へ）。**最初に指定した行のものを動画全体で使う** |
| `milTicker` | 最下部を流れる1文。全行ぶんが連結されて常時流れる |
| `milStep` | いま何問目に挑戦しているか（1始まり）。**指定がない行は直前の値を引き継ぐ** |
| `milWon` | その行で `milStep` 段目を獲得する（ラダーが1段確定して点灯する） |
| `milHook` / `milHookSub` | 冒頭の大テロップ。改行は `\n` で**明示する** |
| `milQ` | 設問文。**13文字以内**にすると1行で大きく出る |
| `milChoices` | 選択肢（4つ）。**出題行・解答行・ライフライン行すべてに同じ内容を書く** |
| `milAnswer` | 正解の位置（**0始まり**）。位置は毎回散らす |
| `milTimer` | 出題行に `true`。制限時間バーがセリフの尺いっぱいで縮む |
| `milShowAnswer` | 解答行に `true`。正解が緑に光ってほかが沈む |
| `milKeep` | 50:50 で残す選択肢の位置（0始まり・2つ）。ほかは文字が消えて枠だけ残る |
| `milAudience` | オーディエンスの投票率（選択肢と同じ並びで4つ）。**演出** |
| `milFinal` | 「ファイナルアンサー？」帯。最終問題だけ |
| `milLifeline` | 使ったライフラインの key（`fifty` / `audience` / `phone`）。以降ずっと×が残る |
| `milLifelineLabel` / `milLifelineSub` | ライフラインスラムの本文と補足行 |
| `milVerdict` / `milVerdictSub` | 中央の判定スタンプと、その上の小ラベル |
| `milFact` | 正解の根拠を1行で（**出典を書く**） |
| `milRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `milFlash` / `milFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `milWin` / `milWinSub` | 賞金獲得スラム（全画面・金の閃光） |
| `milReveal` / `milRevealSub` | リビール帯（宣伝への転換点） |
| `milCta` / `milNote` | 検索バー風CTA と、その下の小さな但し書き |
| `milResult` / `milResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 0〜118 / ライフライン3つ 138〜226 / 挑戦者プレート 146〜218（右） /
賞金ラダー 258〜663（`right: 24`, 幅344, 6段×60＋間9） /
判定スタンプ・テロップ・ライフラインスラム 640〜700 / リビール帯 760〜 /
ループリボン 780〜 / ツッコミ吹き出し 950〜 /
**設問ブロック（ファイナルアンサー帯＋設問カード）は下端 y=1236 に固定して上へ伸びる** /
制限時間バー 1244 / 選択肢 2×2 1268〜1516 / 根拠の1行 1536〜 /
CTA 1156〜 / 注記 1300〜 / ティッカー 最下部92px。

#### 併存フォーマット: 画面当てクイズ型（この画面、なにしてる？）

**映像そのものを問題にする**。過去に2つクイズ型を作っている（3択＋カウントダウン
リング型／○×型。どちらも `config/archive/` に文言だけ残してコンポーネントは削除済み）が、
**どちらも設問がテキスト**で、マイクラの映像は後ろに敷いてあるだけだった。
この型はそこを壊す。聞くことはほぼ1つ——「この画面、なにしてる？」。

そのため**画面の中央には何も置かない**。`QuizScrim` は上下だけを落として中央を
素通しにし、`SceneVisuals` の `screen` モードが**拡大もパンもせず原寸比で
中央のモニターに映す**。いつもの 160%×125% の拡大をかけると肝心のGUIが切れて
問題が成立しないため、この型だけ映像の扱いが根本的に違う。

進行：id1「ここは、よもぎサーバーの生活鯖なのだ」で名乗り（**この型もサーバー名を
伏せない**）、id2「マイクラでできること、どこまで分かる？」で参加を促す。
以降は 1問＝出題行＋解答行の2行で7問。会社の決算／無人のお店／釣れる魚275種／
車／ガチャ／土地は買った／参加費0円。2問ごとにめたんのツッコミを挟む。
最終問題の正解（0円）でトーンが紫→緑に反転し、そのまま
「よもぎ生活サーバー／ぜんぶ、実際にできます」のリビールへ。締めは
「できること、まだあるのだ／何問わかった？ コメントで」でループ。

**この型の芯は「毎回いちばんありえない選択肢が正解」**。数問で視聴者が
「じゃあ一番ヤバいやつを選べばいいのか」と学習するので後半は当たるようになり、
当たること自体が「マイクラでそこまでできる」の証明になる＝遊びと宣伝が同じ動作になる。
最終問題（参加費）だけそのルールを裏切って**いちばんお得な選択肢**が正解。

**この型は「説明してよい」型**。最初からクイズ番組のパロディUIだと分かる茶番なので、
常設メーター（進捗セグメントと難易度★）で残りを明示してよい。

離脱防止・参加の装置：

| 装置 | 役割 |
|------|------|
| 進捗セグメント＋「n / 7」（`quizNo`） | **この型の"あと何"メーター**。1問進むごとに1つ埋まって弾む |
| 難易度★（`quizLevel`） | ★1→★5 と上がっていく。**演出なので注記で明示する** |
| 制限時間バー（`quizTimer`） | 出題行の尺いっぱいで縮む＝答える強制力。残り少ないと赤へ寄る |
| 選択肢ボタン（`quizChoices`） | YouTubeの動画内クイズ機能を模した押せそうなボタン。出題中は脈打ち、解答行で正解が緑に光ってほかが沈む |
| 中央の判定スタンプ（`quizVerdict`） | 正解が斜めに叩き込まれる |
| 「答えは、コメントで」バー | ティッカーのすぐ上に**出題中ずっと**出しっぱなし。2026年の伸びる条件＝参加型の体験設計 |

`QuizChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。進捗セグメントは Main が「前の行の値 → いまの行の値」と
現在行の開始フレームを渡し、進んだ瞬間だけ弾ませる。

**素材の選び方がほかの型と正反対**。この型は素材の中身が問題なので、
**プラグインのGUIが写っている素材ほど良い**（リスポーン型は「あなたの昔のワールド」の
画にならないので避けろと書いてあるが、こちらはGUIそのものが答えになる）。
そのぶん**素材のどこにGUIが映っているかを必ず確かめてから `startFrom` を決める**。
例えば `会社の社員一覧や売上履歴を見ている動画.mp4` は全1803フレームのうち
**帳簿のGUIが映っているのは f≈990〜1170 だけ**で、それ以外は街を歩いているだけ。
`npx remotion ffmpeg -ss <秒> -i <素材> -frames:v 1 -vf scale=300:-1 out.png` で
等間隔に抜いて PIL で並べたコンタクトシートを作ると一度に判断できる。

映像で成立しない設問は**設問のほうを変える**。土地は当初「なにを買ってる？」に
していたが、土地保護の素材にGUIらしい画がなく問題が成立しなかったので、
建築中の画に合わせて「この土地、どうやって手に入れた？」に変えている。

**人狼版（`config/script.werewolf-screenquiz.yaml`）は同じHUD・同じフィールドのまま
題材だけ差し替えてある**（全6問21行）。人狼素材は4本しかないが、
「殺し屋が霊媒師のフリ.mp4」には一人称視点の人狼HUD（右上「あなたの役職は:
殺し屋（人狼陣営）」・役職一覧・COログ・赤いキル表示）がそのまま映っており、
GUIが答えになるこの型と相性がいい。学習ルールは「いちばん物騒な選択肢が正解」に
読み替えて維持（会議／殺された／47種類／殺し屋／処刑）し、最終問題（参加費0円）だけ
お得側に裏切るのは生活サーバー版と同じ。役職数は intro.md の41種類ではなく、
日付つきで新しい all_roles.md（2026年7月19日時点で47）を採用している。
「会議中の風景2.mp4」の f≈390 前後はカメラが黒い人物に塞がれるので出題カットに使わない。

#### スクリプトのフィールド（quiz*）

| フィールド | 用途 |
|-----------|------|
| `quizTone` | `play`（出題中・紫）/ `clear`（全問終了・緑）。**指定した行から後ろに引き継がれる** |
| `quizTitle` | ヘッダの番組タイトル。**最初に指定した行のものを動画全体で使う** |
| `quizTicker` | 最下部を流れる1文。全行ぶんが連結されて常時流れる |
| `quizNo` | 何問目か。**指定がない行は直前の値を引き継ぐ**。進捗セグメントの分母は最大値 |
| `quizLevel` | 難易度（★の数 1〜5）。**指定がない行は直前の値を引き継ぐ**。演出 |
| `quizHook` / `quizHookSub` | 冒頭の大テロップ。改行は `\n` で**明示する** |
| `quizQ` | 設問文。問題ごとに変えてよい（映像で成立しない設問は設問側を変える） |
| `quizChoices` | 選択肢（3つ想定）。**出題行と解答行の両方に同じ内容を書く** |
| `quizAnswer` | 正解の位置（**0始まり**）。位置は毎回散らす |
| `quizTimer` | 出題行に `true`。制限時間バーがセリフの尺いっぱいで縮む |
| `quizShowAnswer` | 解答行に `true`。正解が緑に光ってほかが沈む |
| `quizVerdict` / `quizVerdictSub` | 中央の判定スタンプと、その上の小ラベル |
| `quizFact` | 正解の根拠を1行で（選択肢の下に出る） |
| `quizRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せ |
| `quizFlash` / `quizFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `quizReveal` / `quizRevealSub` | リビール帯（全問終了＝宣伝への転換点） |
| `quizCta` / `quizNote` | 検索バー風CTA と、その下の小さな但し書き |
| `quizResult` / `quizResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 0〜118 / 進捗セグメント 132〜144 / 設問カード 186〜 /
**モニター 372〜941（`SceneVisuals` の `SCREEN_TOP` / `SCREEN_HEIGHT`）** /
判定スタンプ 660〜 / 巨大テロップ 640〜 / リビール帯 700〜 / ループリボン 730〜 /
ツッコミ吹き出し 900〜 / 制限時間バー 1080 / 選択肢 1120〜1496 /
根拠の1行 1516〜 / CTA 1156〜 / 注記 1300〜 /
「答えは、コメントで」バー 最下部92pxのすぐ上（高さ96） / ティッカー 最下部92px。

**モニターの位置を変えるときは `SceneVisuals` 側の定数も一緒に直すこと**
（HUDとモニターは別ファイルにあり、ここだけがフォーマット間で唯一 `SceneVisuals` に
手を入れている箇所）。

#### 併存フォーマット: 巻き戻し型（DAY 365 → DAY 1）

**時間を逆に流す**。これまでの型はすべて時間が前へ進み、事実・証拠・商品・質問・記憶が
1カットずつ積み上がってメーターが動き、結末へ向かう構造だった（リスポーン型のハートは
減るが、あれも時間は前へ進んでいる）。この型は**DAY 365（完成した暮らし）から
DAY 1（なにも持っていない日）へ遡り**、そのあいだ持ち物のチップが1つずつ
画面から消えていく。**既存21型が一度も使っていない「時間経過」軸**。

2026年のショート動画で伸びる企画として名指しされているのが「ビフォーアフター型」と
「裏側・制作過程型（タイムラプス）」で、どちらも定石は**完成品を最初に見せてから
過程に戻る**こと。この型はその構造そのものになっている。

進行：id1 で「ここは、よもぎサーバーの生活鯖なのだ」と名乗り、続けて
めたんの「土地、家、店、会社、車。ぜんぶ持ってるわ」→ ずんだもんの
「年収は、ゼロ円なのだ」という矛盾を畳みかける。そのまま
「巻き戻してみるのだ」で DAYカウンターが減り始める。
車→会社→店→家→土地→釣り竿→仲間→ツルハシ の順にチップが赤く光って
打ち消し線が入り、右へはじけ飛ぶ。id16 で**持ち物ゼロ**になり、
id17 で **DAY 1 の全画面スラム（持ち物 なし）**。そこに「なにも、ない」→
**「1年前は、だれでもここ」**（＝この動画の芯）を重ねる。id20 でトーンが緑に反転し、
剥がしたチップが**同じ順に積み直りながら**宣伝が始まる。締めは
「あなたのDAY 1は？」でループ（最終行で DAY を 365 に戻し、映像も id1 と同じにする）。

**この型はサーバー名を伏せない**（コメント返信型と同じ考え方）。報道型・裁判型・
通販型は正体を伏せて好奇心ギャップを作るが、この型の引っぱりは
**DAYカウンターと剥がれていくチップ**が担うので、名前を隠す必要がない。
**id1 でいきなり名乗る**。設計原則の「冒頭1秒は起ではなく転」からは外れるが、
「なんの動画なのか」を1フレーム目で確定させることを優先している
（そのぶん id2・id3 で矛盾を畳みかけて掴む）。
id20 のリビール帯は正体明かしではなく、**宣伝への転換点の合図**として残している。

この型の宣伝が押し売りにならないのは、**出発点として出るから**。「すごい人の暮らし」で
終わらせず、DAY 1 まで遡って全部剥がしきり、なにも持っていない画をいちばん大きく
見せてから受け皿を出す。id19 より前に宣伝の中身（機能紹介）を1つも入れない。

**この型は「説明してよい」型**。最初から巻き戻しプレイヤーのパロディUIだと分かる
茶番なので、常設メーター（DAYカウンター＋縦レール）で「あと何日で0になるのか」を
明示してよい。**ただし「ここから宣伝です」「立場が変わりました」の類は書かない**。
転換（id20）はトーンの反転（青→緑）・◀◀ が ▶▶ に変わること・チップが積み直り
始めることだけで伝える。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| DAYカウンター（`rwDay`） | **この型の"あと何"メーター**。365 → 1 へ減り、数字が転がって着地する |
| 右端の縦レール | 上端＝DAY 365 / 下端＝DAY 1。つまみが上から下へ降りる。残りが常に見える |
| 持ち物チップ（`rwGot`） | 遡るたびに赤く光り打ち消し線が入って消える。8個 → 0個 → 5個 |
| 記録カード（`rwLog`） | 1カット1日。**上から**滑り込んで前のカードを押しのける（巻き戻しの向き） |
| VHSのトラッキングノイズ | 巻き戻し中だけ走査線と横帯が走る。逆再生を文字で説明せずに済ませる層 |
| 逆向きに流れる記録メモ帯 | ほかの型のティッカーは右→左だが、この型だけ左→右 |

`RewindChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の
`ShopChrome` と同じ）。DAYカウンターとレールは Main が「前の行の値 → いまの行の値」と
現在行の開始フレームを渡し、数字を転がしてから着地させる。
**`RewindScrim` は行のトーンを受け取る**（`renderScrim(index)`）。ドラマ型・リスポーン型と
同じく色そのものを変える型なので、暗くしすぎると素材が真っ黒になってカットが変わった
ことすら分からなくなる。映像が読み取れる濃さで止めること。

チップの解決は Main の `rwChipsResolved` が担う。**一度 start に入ったら、そのあと
トーンが now に戻っても（＝ループ用の最終行）チップは積み上げたままにする**。
ここを毎行のトーンで判定すると最終行だけ持ち物が空になり、ループの継ぎ目が割れる。

チップは横1列なので、**実際の描画幅**で自動縮小している（個数ではなく幅で見るので
「ツルハシ」のような長い語が混ざっても溢れない）。それでも8個程度までにすること。

記録の中身は全部 docs/yomogi で裏が取れる事実にする（土地と家／チェストショップ＝
店番のいらない店／会社制度／釣り275種／車／近距離VC／参加費0円／統合版／24時間）。
**日付と人物はフィクション**なので、CTA下の注記（`rwNote`）で明示すること。

#### スクリプトのフィールド（rw*）

| フィールド | 用途 |
|-----------|------|
| `rwTone` | `now`（完成形・金）/ `rewind`（巻き戻し中・冷たい青）/ `start`（出発点・緑）。**指定した行から後ろに引き継がれる**。転換の合図はこれと ◀◀ / ▶▶ の反転だけが担う |
| `rwTicker` | 最下部の記録メモ帯を流れる1文。全行ぶんが連結されて常時流れる |
| `rwDay` | 何日目か（365 → 1）。**指定がない行は直前の値を引き継ぐ**。レールの上端は最大値 |
| `rwGot` | 持ち物チップ。巻き戻し中は**その行のあいだはまだ手元にあって次の行で消える**、宣伝フェーズでは**その行ではじめて積まれる** |
| `rwLog` / `rwLogLabel` / `rwLogSub` | 記録カードの本文・ラベル（この先 など）・補足。**本文は13文字前後×2行まで** |
| `rwRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せになる |
| `rwFlash` / `rwFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する**。この行では記録カードを描かない |
| `rwOrigin` / `rwOriginSub` | DAY 1 到達スラム（全画面・白フラッシュ）。持ち物が全部消えた瞬間に出す |
| `rwReveal` / `rwRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `rwCta` / `rwNote` | 検索バー風CTA と、その下の小さな但し書き |
| `rwResult` / `rwResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
再生ヘッダ（◀◀・タイムコード）0〜120 / DAYカウンター 138〜300 /
持ち物チップ列 322〜418 / 記録カード 560〜 / テロップ 620〜 / リビール帯 760〜 /
ループリボン 790〜 / CTA 1120〜 / ツッコミ吹き出し 1170〜 / 注記 1276〜 /
記録メモ帯 最下部92px。右端の縦レールは 470〜1250（`right: 22`, 幅18）。
**縦レールとぶつかるので、この型のテロップ類は右マージンを 74 にしてある**。
DAY 1 到達スラムだけは全画面。

記録カード・吹き出しは `layoutLines()` が扱う。1行で収まらないときだけ句読点で2行に割り、
区切り記号がない文は折らずにフォントを縮める。

#### 併存フォーマット: リスポーン型（あなたのワールド、まだ残ってる）

**画面をマイクラのゲームUIそのものにする**。これまでの型はすべて「テレビ番組・
Webサイト・アプリのパロディUI」だった（報道／裁判／通販／コメント欄／街頭インタビュー／
求人サイト）。ドラマ型だけはUIを捨てたが、それも画面の外側の話だった。この型は
体力ハート・空腹ゲージ・経験値バー・ホットバー・進捗トースト・死亡画面という、
**マイクラを触ったことがある人なら説明ゼロで意味が分かる部品**だけで画面を組む。

軸は**共感（relatability）**。2026年のショートで効いているのは作り込みよりも
見ている人自身の記憶に刺さる内容で、既存フォーマットはどれもこの軸を使っていない
（全部ギャップ・茶番・ドラマだった）。ターゲットは「昔マイクラをやっていて、
いつのまにか開かなくなった人」＝**生活サーバーにまだ来ていない人そのもの**。

進行：めたんの「あなたのワールド、まだ残ってるわ」（1秒フック）から、
マイクラの「あるある」を進捗トーストで1つずつ出す。**刺さるたびに体力ハートが1個減る**
（最初の夜／木の家／ダイヤ／洞窟／エンダードラゴン／サーバー立てようぜ／立たなかった／
やることが分からない／閉じた／それきり の10個）。0になると
**「死んでしまった！」の全画面**が出て、そのまま id17・18 の
**「飽きたわけじゃない」「やることが、無くなっただけ」**（＝この動画の芯）を
死亡画面の上に重ねる。id19 で「リスポーン」ボタンが押され、白く飛んで明るい街が現れ、
**ハートが左から順に全回復する**。以降が宣伝で、締めは「最終ログイン、いつ？」でループ。

この型の宣伝が押し売りにならないのは、**復帰の受け皿**として出るから。
「新しいサーバーがあります」ではなく「飽きたんじゃない、やることが無くなっただけ」
と言い切ってから受け皿を見せる順序が要点で、id18 より前に宣伝要素を1つも入れない。
「あるある」はマイクラ一般の記憶であって、よもぎサーバーの話ではない。

**この型は「説明してよい」型**。最初からマイクラUIのパロディだと分かる茶番なので、
常設メーター（ハート10個）で「あと何回で死ぬのか」を明示してよい。
**ただし転換だけは文字で予告・実況しない**。リスポーンの行（id19）にはテロップを置かず、
ボタンの押し込み・白飛び・ハートの回復・カラーグレードの反転（青→暖色）と、
マイクラの実際のシステムメッセージ「スポーン地点を設定しました」だけで伝える。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| 体力ハート10個（`respHp`） | **この型の"あと何"メーター**。刺さるたび1個減り、失う瞬間だけ白く弾けて空になる。画面のふちも赤く脈打つ |
| 進捗トースト（`respMemo`） | 1カット1記憶。**「進捗を達成しました！」なのに体力が減る**ねじれがこの型の芯 |
| 死亡画面（`respDeath`） | マイクラ経験者なら1フレームで意味が分かるので、状況説明のテロップが要らない。id16 で出て id18 まで残る |
| リスポーン（`respSpawn`） | 終わったと見せてから引き戻す転換。回復そのものが合図になる |
| ヘッダの「最終プレイ 1,847日前」 | 出しっぱなし。死亡画面のスコアと最終行の問いかけで2回回収する |
| ホットバーの選択枠 | 削れた回数ぶん右へ進む。二次的な進行表示 |

`RespawnChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の `ShopChrome` と同じ）。
ハートは Main が「前の行の値 → いまの行の値」と現在行の開始フレームを渡し、
減った1個だけを弾けさせる。**`RespawnScrim` は行のトーンを受け取る**（`renderScrim(index)`）。
ドラマ型と同じく色そのものを変える型なので、暗くしすぎると素材が真っ黒になって
カットが変わったことすら分からなくなる。映像が読み取れる濃さで止めること。

死亡画面は**出た瞬間だけ最前面**（`deathHeld` が false）で、持ち越しの行では
不透明度を落として**テロップの下に沈める**。Main の `respDeathResolved` が引き継ぎを解決し、
`respSpawn` のある行で打ち切る。

ハート・空腹はドット絵（9×9のグリッド文字列 → SVGのrect）で描いている。
マイクラの文字は必ず右下に影が落ちるので、UI文字には `MC_SHADOW` を当てること。
これだけで「あの画面」に見える。

**記憶パートに、よもぎサーバー固有のプラグインUIが写った素材を使わないこと**
（チェスト保護・buffコマンド・人工資源などのGUI画面）。「あなたの昔のワールド」の
画にならないうえ、画面が暗い上に文字だらけで読めない。採掘・建築・農業・釣りの
自然な素材を使い、街並み・商店街・会社・車はリスポーン後に取っておく（暗→明の対比になる）。

宣伝パートは全部 docs/yomogi で裏が取れる事実にする（土地と家／チェストショップ＝店／
会社制度／釣り275種／車／近距離VC／統合版／24時間／参加費0円）。

#### スクリプトのフィールド（resp*）

| フィールド | 用途 |
|-----------|------|
| `respTone` | `world`（記憶・冷たい青）/ `spawn`（リスポーン後・暖色）。**指定した行から後ろに引き継がれる**。転換を文字で説明しないので、合図はこれとハートの回復だけが担う |
| `respWorld` / `respLast` | セーブデータ帯のワールド名と「最終プレイ ○日前」。**最初に指定した行のものを動画全体で使う** |
| `respHp` | 残りの体力ハート数。**指定がない行は直前の値を引き継ぐ**。減った行でダメージ演出、増えた行で回復演出が入る。ハートの総数は最大値 |
| `respMemo` / `respMemoSub` | 進捗トーストの本文と補足。**本文は14文字前後×2行まで**（左のスロットぶん幅が減る） |
| `respRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せになる |
| `respFlash` / `respFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する**。この行では進捗トーストを描かない |
| `respDeath` / `respDeathSub` | 死亡画面の見出しとスコア行。**書いた行から respSpawn の行まで出しっぱなし**になる（書いていない行は沈んで背景になる） |
| `respSpawn` / `respSpawnSub` | リスポーンのボタン名と、直後に出るチャットのシステムメッセージ。この行で白く飛ぶ |
| `respReveal` / `respRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `respCta` / `respNote` | 検索バー風CTA と、その下の小さな但し書き |
| `respResult` / `respResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
セーブデータ帯／サーバー帯 0〜150 / 進捗トースト 500〜 / テロップ 620〜 /
リビール帯 760〜 / ループリボン 790〜 / CTA 1120〜 / ツッコミ吹き出し・チャット 1170〜 /
注記 1276〜 / 経験値バー 1380〜 / ハート・空腹 1442〜 / ホットバー 1520〜1624 /
字幕 最下部（`bottomOffset: 150`）。死亡画面とリスポーンだけは全画面。

進捗トースト・吹き出しは `layoutLines()` が扱う。1行で収まらないときだけ句読点で2行に割り、
区切り記号がない文は折らずにフォントを縮める。

#### 併存フォーマット: 求人票・募集要項型（この求人、ホワイトすぎませんか？）

**前半はマイクラ映像を1枚も出さない**。これまでの型（報道・裁判・通販・コメント欄・
街頭インタビュー・ドラマ）に共通していた構造的な前提は「1フレーム目から実映像が出ている」
ことだったが、この型はそこを壊す。前半（id1〜18）は**白い求人サイトの画面だけ**で進み、
視聴者は最後まで「マイクラの動画」だと思わない。

id19「日本には、ないのだ」で**求人票が縦に裂け**、その裂け目から実映像が現れる。
前半で一度もマイクラを見せていないぶん、ここの落差が全部の効きどころになる。
**裂け目から現れる1枚目は必ず明るい昼の街にすること**（暗い素材だと落差が死ぬ）。

ネタは `docs/yomogi/living/supplement/good-standing-company.md` の**優良企業ガイドライン**。
時給5,000YG以上・ノルマは週3時間まで・道具代は会社負担・ミスの弁償を請求しない・
退職を拒否できない・副業を禁止できない・面接でため口禁止・不採用でも必ず連絡・
経験で差別しない・ぼったくり禁止（最低販売価格の1.3倍まで）・新規は24時間返品自由。
**12条すべて実在の条文で、創作した条項はひとつもない**。条項カードに出典（第○条）を
出すのは、2026年に強い「意外な事実型（雑学・知識系）」が**出典明記で信憑性を担保する**型
だから。他人に教えたくなる情報＋コメント欄の議論誘発がこの型の狙い。

**この型は「説明してよい」型**。ドラマ型だけは物語をリアルだと信じてほしいので画面が
説明してはいけないが、この型は最初から求人サイトのパロディUIだと分かる茶番なので、
常設メーター（募集要項 n/12 とスクロールレール）で「あと何件あるのか」を明示してよい。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| 募集要項カウンター＋スクロールレール（`jobNo`） | 1/12 → 12/12。**この型の"あと何"メーター**。右端のつまみが下がっていく |
| 条項カード（`jobTerm`） | 1カット1条項で差し替わる。**ツッコミの行では前の条項を薄く残す**（Main が持ち越す）ので紙面が空にならない |
| ツッコミ吹き出し（`jobRetort`） | 4件ごとに挟んでリズムを作る。立ち絵を使わないので、色と名前タグで話者を示す |
| 「YG」という謎の単位 | id3 で出して id4 で「なに？」と聞き、答えを後半まで引っぱる |
| 社名の伏せ字（株式会社 ███████） | 「どこの会社なのか」を最後まで伏せる |
| 応募ボタン風バー | 最下部で脈打ち続け、細則のティッカーが流れる |

`JobChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の `ShopChrome` と同じ）。
`JobBackdrop` だけは**トーンを受け取る**（前半＝白い紙面、後半＝実映像の下地）。
`JobScrim` はほかの型より**薄く**してある（映像がはっきり見えないと落差が死ぬため）。

前半の行には `visual` を**書かない**。実映像が1枚でも混ざるとこの型は成立しない。
条項カードは `jobTone` が `real` になった時点で捨てる（実映像の上に白いカードが残らないよう
Main 側で切っている）。転換の行（id19）だけ、紙が開ききるまでの間が要るので
`pauseAfter` を正の値（12）にする。

#### スクリプトのフィールド（job*）

| フィールド | 用途 |
|-----------|------|
| `jobTone` | `posting`（青・求人サイト・実映像なし）/ `real`（緑・実在・実映像あり）。**指定した行から後ろに引き継がれる**。背景そのものが切り替わる |
| `jobSite` / `jobTitle` | ヘッダのサイト名と求人カードの職種名。**最初に指定した行のものを動画全体で使う** |
| `jobTicker` | 最下部を流れる細則。全行ぶんが連結されて常時流れる |
| `jobNo` | 何件目の募集要項か。**指定がない行は直前の値を引き継ぐ**。レールの分母は最大値 |
| `jobTerm` / `jobTermLabel` / `jobTermSub` | 条項カードの本文・ラベル（給与／労働時間／待遇／採用／取引）・出典。**本文は12文字前後×2行まで** |
| `jobStamp` | 条項カードに斜めに押される丸スタンプ（ホワイト / 実在 / 全12件） |
| `jobRetort` | ツッコミ吹き出し。めたんは右寄せ、ずんだもんは左寄せになる |
| `jobFlash` / `jobFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する**。この行では条項カードを描かない |
| `jobBreak` / `jobBreakSub` | **求人票が裂ける転換スラム**。この行から実映像が現れる |
| `jobReveal` / `jobRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `jobCta` / `jobNote` | 検索バー風CTA と、その下の小さな但し書き |
| `jobResult` / `jobResultSub` | ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
サイトヘッダ 0〜118 / 求人カード見出し 138〜305 / 件数カウンター 322〜 /
スクロールレール 396〜（下端から250） / 条項カード 560〜 /
テロップ 620〜 / 転換スラム 700〜 / リビール帯 760〜 / ループリボン 790〜 /
CTA 1206〜 / 注記 1338〜 / ツッコミ吹き出し 1236〜 /
応募ボタン風バー 最下部128px（後半は最下部92pxのティッカー帯に変わる）。

条項カード・吹き出しは `layoutLines()` が扱う。1行で収まらないときだけ句読点で2行に割り、
区切り記号がない文は折らずにフォントを縮める。

#### 併存フォーマット: 縦型ショートドラマ・逆転劇型（まだマイクラやってるの？）

**画面まるごとドラマ**にする。これまでの型（報道・裁判・通販・コメント欄・街頭インタビュー）が
すべて「パロディUI＋茶番」だったのに対し、この型の核は**人間関係と感情の反転**にある。
2026年のショート動画でいちばん伸びているのは60〜90秒で完結する
**縦型ショートドラマ（micro-drama）**で、ジャンルは復讐・ざまぁ・どんでん返しが強い。
その構造をそのまま借りている。

物語は一直線。めたんが「まだマイクラなんてやってるの？」と見下すところから始まり
（＝前置きゼロで、いちばん強い煽りを1行目に置く）、ずんだもんが淡々と事実
（会社／社員6人／無人の店／釣り275種／家／車／近距離VC）を並べていくたびに立場が削れ、
最後にめたんが「私、なにも持ってない」と折れる。その直後に本人が
「それ、どこでできるの？」と聞くので、**宣伝が押し売りにならない**のがこの型の要点。

**この型だけは、画面が物語を説明してはいけない。**
初版には《逆転まで 00:34》のカウントダウン帯と「この後、立場が入れ替わります」の一文を
常設で置き、シークバーには逆転ポイントの ◆ マーカーを、逆転の行には「立場、逆転」の
スラムを出していたが、これらは物語の外側から「これから逆転が起きます」「いま逆転しました」と
実況してしまうもので、**ドラマの枠組みが露出して「胡散臭い作り話」に見えてしまう**。
すべて撤廃済み。ほかの型（心証メーター・未回答件数・値札・取材ドット）は最初から茶番だと
分かっているので説明してよいが、ドラマだけは別だと考える。

**人狼版（`config/script.werewolf-drama.yaml`）はドラマの軸を差し替えてある。**
「見下し → 立場の逆転」を人狼でやると勝ち負けの自慢話にしかならず宣伝に着地しないので、
**疑心暗鬼と吊り間違い**でやる。めたんが「あなた、人狼でしょ」と決めつけ、弁明するほど
疑いの根拠（`dramaFact`＝アリバイなし／怪しいCO／目撃者／証拠なし／6票）が積まれ、
投票して処刑してから「本当に霊媒師だった」と知る。最悪の後味まで落としてから
「疑うのも、疑われるのも、ぜんぶゲームなのだ」で空気を反転させ、
「これ、なんてゲームなの？」を導線に宣伝へ。締めは「次は、あなたが疑われる」でループする。
**同じHUD・同じフィールドのまま、ドラマの主題だけを題材に合わせて作り替えるのがこの型の転用手順**。

**逆転は暗示的に起こす。** 転換点の行（生活サーバー版は `id18`、人狼版は `id21`）も
ほかと同じ `dramaLine` だけを出し、
`dramaTone: turn` によるカラーグレードとUIアクセント色の反転・ごく短い白フラッシュ
（Main が「前の行とトーンが変わったか」を見て自動で入れる）・BGMの切り替えだけで
「空気が変わった」と伝える。効果音も「ドン」と決めず、息を呑む程度にとどめる。

締めは「第2話　あなたの番／まだマイクラ、やってる？」の**次回予告リボン**で、
ループ（冒頭の煽りに戻る）とコメント誘発を同時に満たす。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| 事実チップ列（`dramaFact`） | 会社→店→魚275種→家→車→0円 と積まれ、立場の差が可視化される。**この型で唯一の常設メーター**で、結末は予告せず「差が開いていく」だけを見せる |
| ヘッダのエピソードタイトル | 答えの出ていない問いかけ（「まだマイクラやってるの？」）をそのまま置く |
| シークバー（最下部） | 配信アプリ風の進行表示。残りが見えるので離脱しにくい |
| カラーグレード（`DramaScrim`） | 前半は冷たい青＋強いビネット、逆転後は暖色＋明るく開く |
| 章タイトルカード（`dramaChapter`） | 「それでも、彼女は認めない」。折れる直前に一段ためる（三人称ナレーションなので予告にはならない） |
| 心の声（`dramaMono`） | 「私より、生活してる」。決定的な一行にだけ使う |
| ドラマ字幕（`dramaLine`） | **この型だけ字幕が主役**。話者タグ付きで全行に出す |

`DramaChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の `ShopChrome` と同じ）。
事実チップは Main が「その行までに出そろった配列」と「前の行までの個数」を渡し、
増えた1個だけを弾ませる。**チップは横1列なので短い語で6個まで**にすること
（7個以上になるとフォントを自動で縮めるが、収まらなくなる）。

**`DramaScrim` だけは行のトーンを受け取る**（`renderScrim(index)`）。ほかの型の暗幕は
明るさを落とすだけだが、この型では色そのものを変えて感情の反転を映像でも伝える。

ずんだもんの発言は**全部 docs/yomogi で裏が取れる事実**にする（会社制度／チェストショップ＝
無人販売所／釣り275種／土地と家／車／近距離VC／参加費0円／統合版／24時間）。
登場人物とエピソードは**フィクション**。CTA下の注記（`dramaNote`）で明示すること。
BGMのsegments切り替え位置（`fromLineId`）は逆転の行（`id18`）に合わせる。

#### スクリプトのフィールド（drama*）

| フィールド | 用途 |
|-----------|------|
| `dramaTone` | `tense`（青・逆転前）/ `turn`（金・逆転後）。**指定した行から後ろに引き継がれる**。逆転を文字で説明しないので、転換点はこれだけが担う（切り替わった行に白フラッシュが自動で入る） |
| `dramaTitle` / `dramaEpisode` | ヘッダ帯のエピソードタイトルと話数バッジ。**最初に指定した行のものを動画全体で使う** |
| `dramaSpeaker` | 話者タグの表示名（省略時はキャラクター名） |
| `dramaLine` | ドラマ字幕。**テロップ系のフィールドがない行には必ず書く** |
| `dramaMono` | 心の声（斜体・画面中央）。決定的な一行にだけ使う |
| `dramaJab` | 見下しセリフの極太スラム（1秒フック）。出た瞬間に画面が青く振れる |
| `dramaChapter` | 章タイトルカード（時間経過・場面転換） |
| `dramaFact` | 事実チップ。**短い語で6個まで**。積み上がって立場の差を可視化する |
| `dramaFlash` / `dramaFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `dramaReveal` / `dramaRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `dramaCta` / `dramaNote` | 検索バー風CTA と、その下の小さな但し書き |
| `dramaResult` / `dramaResultSub` | 次回予告リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
上下のシネマ黒帯 上46px・下92px（下はシークバーが兼ねる） /
ヘッダ帯 62〜154 / 事実チップ列 178〜256 /
心の声 560〜 / テロップ・煽りスラム 640〜 / リビール帯 760〜 /
次回予告リボン 790〜 / 章カード 840〜1050 / CTA 1206〜 / 注記 1338〜 /
ドラマ字幕 1430〜 / シークバー 最下部92px。

ドラマ字幕・テロップは `layoutLines()` が扱う。1行で収まらないときだけ句読点で2行に割り、
区切り記号がない文は折らずにフォントを縮める。

#### 併存フォーマット: 街頭インタビュー・突撃取材型（街の人7人に聞いてみた）

**画面まるごとロケVTR**にする。これまでの型（報道・裁判・通販・コメント欄）が
「パロディUI＋茶番」だったのに対し、この型の核は**映像と音声のズレ**にある。
映っているのはマイクラなのに、聞こえてくる会話は完全に現実の街頭インタビュー。

**id1 で「マイクラのワールドにいる人に突撃取材してみた」と状況を言い切る**。
ここを省くと、ズレが「面白い」ではなく「よく分からない」になって離脱される
（初版はこれで分かりにくかった）。明示するのは*マイクラで取材している*ことだけで、
サーバー名は id17 まで伏せる。音声が短いぶんは `intvFlash` のデカ文字で画面を持たせる。

そのうえで id2 の質問「いま、何してたんですか？」への答えが
「会社の決算です。社員が6人いて」なので、視聴者は「マイクラの中で決算？」となって
引き込まれる。以降は**1人1カット**で答えがどんどんおかしくなる
（無人店／釣り275種／畑／車／近距離VC）。

常設の**《取材 n人目》ドット列は7個**あり、6人目まで埋まったところで止まる。
「7人目は誰なんだ」という open loop を冒頭のヘッダ帯（「マイクラの街の人7人に聞いてみた」）
から張り、最終行で**「7人目は、あなた」**と視聴者に向けて回収する。この1枚が
ループ（冒頭に戻る）とコメント誘発（「いま、何してた？」）を同時に満たすのが要点。
ドット列は id2（1人目に声をかける行）から出す。id1 では出さない。

転換は「ここ、入るのにいくらかかるの？」→**全員「0円」**の取材終了スラム（白フラッシュ）。
ここで赤「REC」→緑「取材終了」にトーンが変わり、続く id16「この街、どこにあるの？」が
視聴者の疑問を代弁する形で宣伝への導線になる（`InterviewHud.tsx`）。

**この型はサーバー名を id17 まで伏せる**。コメント返信型とは逆で、
「どこの世界の話なのか」という好奇心ギャップが引っぱりの主軸になるため。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| ビューファインダーUI（`InterviewChrome`） | RECランプの明滅・進み続けるタイムコード・手ブレする四隅マーカー・カチンコ柄の取材メモ帯が**常時**動く |
| 取材カウンター＋ドット列（`intvCount`） | 1人目→7人目。**最後の1個が空いたまま残る**のが最大の引っぱり装置 |
| トーン切替（`intvTone`） | 取材終了以降 赤「REC」→緑「取材終了」。ドット列も緑に反転する |
| 回答者プレート＋極太テロップ（`intvName` / `intvAnswer`） | 1人1カット。プレートが変わることで別人だと分かる |
| リアクションスタンプ（`intvReaction`） | 集中線つきの「275種!?」「車!?」。数字が出た行だけに使う |
| 手持ちカメラの揺れ | `Main` が `SceneVisuals` に `handheld` を渡し、**映像側にも**揺れをかける |

`InterviewChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の `ShopChrome` と同じ）。
取材カウンターは Main が「前の行の値 → いまの行の値」と現在行の開始フレームを渡し、
変わった瞬間だけドットを弾ませる。ドットの個数はスクリプト中の `intvCount` の最大値。

**手ブレは `handheld` フラグで opt-in**にしてある（`SceneVisuals` の `handheldOffset()`）。
ほかのフォーマットの見た目を変えないため、`Main.tsx` が `FORMAT === "interview"` のときだけ渡す。

回答は**全部 docs/yomogi で裏が取れる事実**にする（会社制度／チェストショップ＝無人販売所／
釣り275種／農業／車／近距離VC／参加費0円／統合版／24時間）。
回答者は**全員架空の人物**。実在の街頭インタビューだと誤認させないよう、
CTA下の注記（`intvNote`）で「※取材風の演出です」と明示すること。
BGMのsegments切り替え位置（`fromLineId`）は取材終了の行（`id15`）に合わせる。

#### スクリプトのフィールド（intv*）

| フィールド | 用途 |
|-----------|------|
| `intvTone` | `rec`（赤・REC）/ `wrap`（緑・取材終了）。**指定した行から後ろに引き継がれる** |
| `intvTicker` | 最下部の取材メモ帯を流れる1文。全行ぶんが連結されて常時流れる |
| `intvCount` | 何人目の取材か。**指定がない行は直前の値を引き継ぐ**。ドットの個数は最大値 |
| `intvQuestion` | 取材班の質問（白い吹き出し。マイクアイコン付き） |
| `intvName` / `intvRole` | 回答者の仮名と肩書き。**実在しない架空の人物**にする |
| `intvAnswer` / `intvAnswerSub` | 回答の極太テロップ（黄色＋黒縁）と補足行（字幕の代わりに読ませる） |
| `intvReaction` | 集中線つきのリアクションスタンプ（「!?」「275種!?」など） |
| `intvFlash` / `intvFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `intvWrapUp` / `intvWrapUpSub` | 取材終了スラム（トーンを解除する転換点。白フラッシュ付き） |
| `intvReveal` / `intvRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `intvCta` / `intvNote` | 検索バー風CTA と、その下の小さな但し書き |
| `intvResult` / `intvResultSub` | 結果＝ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 56〜160 / 取材カウンター＋ドット列 178〜288 / 質問吹き出し 320〜 /
リアクションスタンプ 660〜960（右寄せ） / テロップ・取材終了・リビール 620〜1010 /
回答者プレート 1000〜1110 / 回答テロップ 1132〜 / CTA 1206〜 / 注記 1338〜 /
取材メモ帯 最下部92px。四隅のビューファインダーマーカーは上320・下128。

回答テロップは `layoutTelop()` が扱う。1行で収まらないときだけ句読点で2行に割り、
区切り記号がない文は折らずにフォントを縮める（語の途中で折れて読みにくくなるのを避けるため）。

#### 併存フォーマット: コメント返信・反論処理型（来た質問、全部答える）

**前半で煽り→後半で落ち着かせて宣伝**の勝ちパターンを、番組パロディではなく
**画面まるごとコメント欄**でやる。冒頭1秒でいちばん強い煽りコメント
「マイクラ、もうやることなくない？」が画面を占拠し（＝クライマックスから始める）、
2行目で「よもぎサーバーに来る質問なのだ」と**何の動画かを明示**してから、
めたんとずんだもんが1件ずつ返信していく。常設の**《未回答 6件》カウンター**が
1件ずつ減っていくので「0件になったら終わるのか」で最後まで引っぱれる。
最後の質問「で、いくらかかるの？」への回答「0円」で未回答0件＝**回答完了スラム**
（白フラッシュ）を出してトーンを解除し、その直後に**新着1件「で、どうやって入るの？」**が
飛び込む（終わったと見せてから引き戻すギャップ）。それがそのまままとめ帯→検索CTAの動線になる。
視聴者の質問に答える形で宣伝するので押し売りにならないのがこの型の要点。
締めは「質問、まだある？／コメントで待ってる」でコメント誘発＋冒頭のコメントへループ（`ReplyHud.tsx`）。

**サーバー名は伏せない**。ほかのフォーマット（報道・裁判・通販）は正体を伏せて
好奇心ギャップを作るが、この型でそれをやると「結局なんの動画なのか」が最後まで伝わらず
趣旨が不明になる。引っぱりは名前ではなく**未回答カウンター**が担うので、
ヘッダ帯（`ReplyHeader` に固定文字列「よもぎサーバーに来た質問」）と id2 のテロップで
最初から明示する。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| コメント欄UI（`ReplyChrome`） | 返信中ランプ・背景を上に流れるコメント片・最下部の入力欄バーが**常時**動く |
| 未回答カウンター＋解決バー（`replyPending`） | 6→0。「あと何件」で引っぱり、新着で1に戻る瞬間が最大のギャップ |
| トーン切替（`replyTone`） | 回答完了以降 赤「返信中」→緑「解決ずみ」。赤ビネットも消えて安堵に転じる |
| 質問カード＋返信カード（`replyQuestion` / `replyAnswer`） | 上下2枚組。回答行では質問カードが小さく残るので文脈を見失わない |
| 解決スタンプ（`replyStamp`） | 回答のたびに緑の丸印が回りながら押される |

`ReplyChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（通販型の `ShopChrome` と同じ）。
未回答カウンターは Main が「前の行の値 → いまの行の値」と現在行の開始フレームを渡し、
変わった瞬間だけ弾ませる。解決バーの分母はスクリプト中の `replyPending` の最大値。

回答完了スラム（id14）で緊張を解いてから、新着コメント（id15）→ まとめ帯 `replyReveal`（id16）で
正式名称と条件（統合版・0円・24時間）を大きく出す。
字幕は検索CTA（id17）の行だけ。**回答行にも `replyQuestion` を書く**（質問カードが小さく上に残る）。
回答は**全部 docs/yomogi で裏が取れる事実**にする（家/店/会社/統合版スマホSwitch/24時間/近距離VC/
土地保護・チェスト保護/釣り275種/車/参加費0円）。背景を流れるコメント片は全行の `replyQuestion` から
自動生成されるが、新着コメント（`replyNew` のある行）の質問だけは冒頭からネタバレしないよう除外される。
BGMのsegments切り替え位置（`fromLineId`）は回答完了の行（`id14`）に合わせる。

`startFrom` は**素材の長さ（`public/content/video-durations.json`）を必ず確認**して決める。
開始位置＋そのセリフの尺が素材の終端を越えると、カットの後半が静止したまま流れる。
**`video-durations.json` の値は 秒×30 で出しているので、30fps でない素材では実フレーム数より
多く出る**。`生活ワールドの街並みを散策している動画.mp4` は 3565 と記録されているが
実体は 118.84秒 / 29.91fps ＝ 3554フレームで、`startFrom: 3200` を指定したら
レンダリングが `Compositor error: No frame found at position` で落ちた。
**終端から600フレームぶんは空けておく**（この素材なら 2900 以下）。
**`video-durations.json` に載っていても実ファイルがあるとは限らない**（過去に削除された素材の
エントリが残っていて、レンダリングが404で落ちたことがある）。`ls public/content/<フォルダ>` で
実在を確かめるか、先に `npm run scan-durations` を流して掃除しておくこと。
マイクラ人狼で実在する動画素材は `会議中の風景.mp4` / `会議中の風景2.mp4` /
`殺し屋が霊媒師のフリ.mp4` / `霊媒師で市民勝利.mp4` の4本だけ（`.mkv` は編集用の生ファイルで、
ブラウザで再生できないため使わない）。
また `npm run voices` は `src/data/script.ts` を読むので、**版を差し替えたら
`npm run sync-script` を先に実行**しないと前の版のセリフで音声を作ってしまう
（`npm run voices` は内部で sync-script を「後に」呼ぶだけなので、先の1回は自分で打つ）。

#### スクリプトのフィールド（reply*）

| フィールド | 用途 |
|-----------|------|
| `replyTone` | `flame`（赤・返信中）/ `calm`（緑・解決ずみ）。**指定した行から後ろに引き継がれる** |
| `replyTicker` | 最下部の入力欄バーを流れる1文。全行ぶんが連結されて常時流れる |
| `replyPending` | 未回答の件数。**指定がない行は直前の値を引き継ぐ**。0で緑に反転 |
| `replyUser` | 質問コメントの投稿者名。**実在しない架空のハンドル**にする |
| `replyQuestion` | 質問コメント本文。回答行にも同じ文を書くと小さく上に残る |
| `replyLikes` | 質問コメントのいいね数表示（例: 2.4万）。演出用の架空の数字 |
| `replyAnswer` / `replyAnswerSub` | 返信カード本文と補足行（字幕の代わりに読ませる） |
| `replyNew` | 黄色い「新着コメント」バッジ。この行の質問は背景のコメント片から除外される |
| `replyStamp` | 緑の丸い「解決」スタンプ |
| `replyFlash` / `replyFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `replyClear` / `replyClearSub` | 回答完了スラム（トーンを解除する転換点。白フラッシュ付き） |
| `replyReveal` / `replyRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `replyCta` / `replyNote` | 検索バー風CTA と、その下の小さな但し書き |
| `replyResult` / `replyResultSub` | 結果＝ループ用リボン（コメント誘発の一言を `Sub` に置く） |

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 56〜160 / 未回答カウンター 178〜296 / 質問カード 430（回答あり）or 560（質問だけ） /
テロップ・回答完了・リビール 600〜1010 / 解決スタンプ 1088〜1338（右寄せ） /
CTA 1206〜 / 注記 1338〜 / 入力欄バー 最下部92px。

コメント本文は `layoutComment()` が扱う。1行で収まらないときだけ句読点で2行に割り、
区切り記号がない文は折らずにフォントを縮める（語の途中で折れて読みにくくなるのを避けるため）。

#### 併存フォーマット: テレビショッピング・通販型（この暮らし、まるごと通販）

**前半でハイテンション → 後半でトーンダウンして宣伝**の勝ちパターン。深夜の通販番組の茶番として始める。
「本日の商品は、この暮らし、まるごとです」で開始（フック）、MC（めたん）とアシスタント（ずんだもん）が
セット内容を1カット1点ずつ紹介していく（マイホーム→無人店→会社→釣り275種→車）。
常設の**値札「お値段 ？？？円」がぶら下がり続け**、セット内容カウンターが 1点→6点 と弾みながら増える。
特典「今ならガチャも付いてくる」で追い打ち→「気になるお値段は」ドラムロール→
**「0円」の値段発表スラム**（白フラッシュ付き）で緊張を解く。
ここで通販トーンを解除し（赤「生放送」→緑「ご案内」、値札は緑の「参加費 0円」に反転）、
正体＝よもぎサーバーの生活サーバーだと明かして検索CTAへ着地する（`ShopHud.tsx`）。
締めは「お次の商品は、あなたの新生活」で冒頭の商品発表に戻してループ。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| 通販UI（`ShopChrome`） | 生放送ランプ・紅白のテント柱・流れるティッカー・暖色ビネットが**常時**動く |
| 値札（`shopPrice`） | 「お値段 ？？？円」が常設で揺れ続ける。「結局いくらなのか」で最後まで引っぱれる |
| セット内容カウンター（`shopCount`） | 1点→6点。増えるたび数字が弾む |
| トーン切替（`shopTone`） | 値段発表以降 赤「生放送」→緑「ご案内」。ビネットも消えて安堵に転じる |
| 商品プレート（`shopItem`）＋セットINシール（`shopStamp`） | 1カット1点、左からワイプ。字幕の代わりに読ませる |
| 特典バースト（`shopBonus`） | 集中線＋黄色スラムで「今ならさらに！」の追い打ち |

`ShopChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く（裁判型の `CourtChrome` と同じ）。
値札とカウンターは Main が「前の行の値 → いまの行の値」と現在行の開始フレームを渡し、
変わった瞬間だけスラム・バウンドさせる。

**前半（id1〜8）はサーバー名を出さない**。値段発表スラム（id9）で緊張を解いてから
`shopReveal` で正体を明かす。字幕は検索CTAの行だけ。
セットに入れる"商品"は**全部 docs/yomogi で裏が取れる機能**にする（土地/家/店/会社/釣り275種/ガチャ/車）。
値札＝参加費の意味なので、土地などゲーム内通貨で買うものを「無料で付いてくる」と
誤解させる文言にしない（ティッカー側で「土地を買って」と補足する）。
BGMのsegments切り替え位置（`fromLineId`）は値段発表の行（`id9`）に合わせる。

#### スクリプトのフィールド（shop*）

| フィールド | 用途 |
|-----------|------|
| `shopTone` | `live`（赤・生放送）/ `info`（緑・ご案内）。**指定した行から後ろに引き継がれる** |
| `shopTicker` | 最下部ティッカーの1文。全行ぶんが連結されて常時流れる |
| `shopPrice` | 常設の値札の文字列（？？？円 → 0円）。**指定がない行は直前の値を引き継ぐ**。「？」を含まない値になると緑の「参加費」表示に反転 |
| `shopCount` | セット内容の点数。**指定がない行は直前の値を引き継ぐ** |
| `shopFlash` / `shopFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `shopBonus` / `shopBonusSub` | 特典スラム（黄色バースト＋集中線。サブは赤い丸バッジ「今ならさらに！」） |
| `shopItem` / `shopItemLabel` | 下部の商品プレート本文と左のラベル（セット1 / 特典 / お知らせ） |
| `shopStamp` | ギザギザのお得シール（セットIN など） |
| `shopPriceSlam` / `shopPriceSlamSub` | 値段発表スラム（通販トーンを解除する転換点。白フラッシュ付き） |
| `shopReveal` / `shopRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `shopCta` / `shopNote` | 検索バー風CTA と、その下の小さな但し書き |
| `shopResult` | 結果＝ループ用リボン（冒頭の商品発表に戻す) |

縦配置・`fitFontSize()`・字幕抑止のルールは裁判型と共通（下記参照）。

#### 併存フォーマット: 裁判・尋問型（マイクラの中で会社を経営してる）

**前半で緊張 → 後半でトーンダウンして宣伝**の勝ちパターン。法廷茶番として始める。
「被告人はマイクラの中で会社を経営していると証言」という一文で開廷し（フック）、検察官（めたん）が
「そんなわけがない＝ホラ吹きの疑い」で起訴。被告人（ずんだもん）が「異議あり！」と争い、
証拠映像を1つずつ提出していく。証拠が採用されるたび赤い丸印で「事実」と認定され、
常設の**《有罪の心証》メーターが 98%→80→58→36→18 と削れていく**（前半＝緊張・茶番）。
最終尋問「参加費はいくら？」→「0円」で心証0%、判決は**無罪**。
ここで法廷トーンを解除し、正体＝よもぎサーバーの生活サーバーだと明かして
穏やかな機能紹介＋検索CTAへ着地する（`CourtHud.tsx`）。
締めは「次の被告人は、あなた」で冒頭の開廷に戻してループ。

離脱防止の装置：

| 装置 | 役割 |
|------|------|
| 法廷UI（`CourtChrome`） | 公判中ランプ・流れる速記録ティッカー・木の羽目板・赤いビネットが**常時**動く |
| 有罪の心証メーター（`courtGuilt`） | 98%→0% へ落ちていく1本の線。「0%になったら何が起きるのか」で最後まで引っぱれる |
| トーン切替（`courtTone`） | 判決以降 臙脂「公判中」→緑「閉廷」。赤ビネットも消えて安堵に転じる |
| 証拠プレート（`courtLower`）＋認定スタンプ（`courtStamp`） | 1カット1証拠、左からワイプ。字幕の代わりに読ませる |
| 発言者プレート（`courtRole`） | 裁判長／検察官／被告人。めたんの一人二役（検察官↔裁判長）も成立させる |

`CourtChrome` は**セリフごとの Sequence の外側**（Main 直下）に置く。グローバルなフレームで
動かさないと、カットのたびにティッカーと心証メーターがリセットされて途切れる。ティッカー文字列は
全行の `courtTicker` を Main が連結して1本にする。心証メーターは Main が
「前の行の値 → いまの行の値」と現在行の開始フレームを渡し、セリフの頭でバネ補間して動かす。

**前半（id1〜10）はサーバー名を出さない**。「どこの世界の話なのか」という好奇心ギャップだけで
引っぱり、判決スラム（白フラッシュ付き）で緊張を解いてから `courtReveal` で正体を明かす。
字幕は情報が二重になるのを避けて**検索CTAの行だけ**出す（テロップ・証拠プレートが画面テロップを兼ねる）。
サーバー側の事実（無料・24時間・統合版・土地/家/店/会社/釣り275種/車）だけを `docs/yomogi` で裏を取ること。
起訴の"容疑"は**全部あとで事実と認定できるもの**にする（証拠の裏が取れない容疑を入れない）。
BGMのsegments切り替え位置（`fromLineId`）は判決の行（`id11`）に合わせる。

#### スクリプトのフィールド（court*）

| フィールド | 用途 |
|-----------|------|
| `courtTone` | `trial`（臙脂・公判中）/ `verdict`（緑・閉廷）。**指定した行から後ろに引き継がれる** |
| `courtTicker` | 最下部の速記録ティッカーの1文。全行ぶんが連結されて常時流れる |
| `courtGuilt` | 有罪の心証（0〜100）。**指定がない行は直前の値を引き継ぐ**。`verdict` になると非表示 |
| `courtRole` | 発言者プレートの肩書き（裁判長 / 検察官 / 被告人）。氏名はキャラクターから自動 |
| `courtFlash` / `courtFlashSub` | 巨大テロップ（積みテロップ）。改行は `\n` で**明示する** |
| `courtCharge` / `courtChargeSub` | 起訴状ボード（罪名＋補足。赤い「起訴」の丸印が押される） |
| `courtObjection` | 「異議あり！」スラム（集中線つき） |
| `courtLower` / `courtLowerLabel` | 下部の証拠プレート本文と左のラベル（証拠1 / 尋問 / 最終尋問 / お知らせ） |
| `courtStamp` | 赤い丸印の認定スタンプ（事実 など） |
| `courtJudgment` / `courtJudgmentSub` | 判決スラム（法廷トーンを解除する転換点。白フラッシュ付き） |
| `courtReveal` / `courtRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `courtCta` / `courtNote` | 検索バー風CTA と、その下の小さな但し書き |
| `courtResult` | 結果＝ループ用リボン（冒頭の開廷に戻す） |

テロップは折り返さず1行で見せるため、`fitFontSize()` が幅に合わせて自動で縮める。
それでも `courtLower` は**12文字前後まで**にすると大きく出せる（ラベルのぶん幅が減る）。

画面の縦配置は固定で決めてある。パーツを足すときはここを崩さないこと：
ヘッダ帯 56〜160 / 心証メーター 178〜306 / 発言者プレート 322〜 /
テロップ・起訴状・判決・リビール 520〜1010 / 認定スタンプ 930〜1190（右寄せ） /
証拠プレート・CTA 1206〜 / 注記 1338〜 / 速記録ティッカー 最下部92px。

#### 併存フォーマット: 緊急速報・報道型（【速報】マイクラに謎の巨大都市）

マイクラ人狼版の現行フォーマットでもあるため `NewsHud.tsx` を残している。
ニュース速報として始め、キャスター（めたん）と現場リポーター（ずんだもん）が中継で騒ぎ、
生活の痕跡が次々見つかる（前半＝緊張）。`newsCorrection`（訂正スラム）で緊張を解いてから
`newsReveal` で正体を明かし、赤「速報」→緑「お知らせ」にトーンダウンして宣伝へ着地する。
人狼版は同じ骨格で事件だけ差し替えたもの（毎週土曜の夜に住民が消える村→マイクラ人狼イベント）。

| フィールド | 用途 |
|-----------|------|
| `newsTone` | `breaking`（赤・速報）/ `calm`（緑・お知らせ）。**指定した行から後ろに引き継がれる** |
| `newsTicker` | 最下部ティッカーの1文。全行ぶんが連結されて常時流れる |
| `newsFlash` / `newsFlashSub` | 巨大ヘッドライン（積みテロップ）。改行は `\n` で**明示する** |
| `newsLive` | LIVE中継バッジの文言（例: 現場から中継） |
| `newsLower` / `newsLowerLabel` | 下部ニューススーパー本文と左のラベル（中継 / 独自 / 続報 / 解説 / お知らせ） |
| `newsExpert` / `newsExpertRole` | 専門家プレート（名前・肩書き） |
| `newsUpdate` | 黄色い「続報」スタンプ |
| `newsCorrection` / `newsCorrectionSub` | 訂正スラム（速報トーンを解除する転換点。白フラッシュ付き） |
| `newsReveal` / `newsRevealSub` | リビール帯（正体明かし＝宣伝への転換点） |
| `newsCta` / `newsNote` | 検索バー風CTA と、その下の小さな但し書き |
| `newsResult` | 結果＝ループ用リボン（冒頭の速報に戻す） |

#### 動画素材（visual）

- **全行に映像を敷く**。1項目1カットで割り当て、1秒ごとにカットが変わる。
  **例外は求人票型の前半**で、そこだけは「マイクラ映像をまだ見せない」こと自体が企画なので
  `visual` を書かない（裂け目から実映像が出る落差を作るため）
- 映像の上には自動で暗幕（`LieScrim` / `MarketScrim` / `ExamScrim` / `RailScrim` / `JoinScrim` / `PromoScrim` / `MillionScrim` / `QuizScrim` /
  `RewindScrim` / `RespawnScrim` /
  `JobScrim` / `DramaScrim` / `InterviewScrim` / `ReplyScrim` / `ShopScrim` / `CourtScrim` /
  `NewsScrim`）がかかる（テロップを読ませるため）。
  **`RailScrim` は上部（路線図の下端 y≈430）までを強く落として、そこから下は一気に開ける**。
  ここを長く引きずると昼の素材まで夜に見える。走行中だけ薄い光の筋が流れる。
  **`JoinScrim` だけは `backdropFilter: blur()` で映像をぼかす**（手順パートのみ）。
  ぼけが取れることが「入った」の合図になるので、ここを暗さだけの実装に戻さないこと。
  **`PromoScrim` は全型でいちばん薄い**（素材そのものが商品カタログなので隠すものがない）。
  **`MillionScrim` はスタジオを模して四隅を強く落とす**ので、屋内の暗い素材を敷くと
  画が潰れる。この型では明るい昼の素材を選ぶこと
  `DramaScrim` / `RespawnScrim` / `RewindScrim` は行のトーンを受け取り、暗さと色味まで変える。
  **`QuizScrim` だけは中央をほとんど素通しにする**（映像の中身が問題そのものなので、
  沈めると問題が読めなくなる）
  `JobScrim` はほかより薄い（映像の出現そのものがオチなので隠しすぎない）
- 素材には座標表示（上端）と体力ゲージ・ホットバー（下端）が写り込んでいるので、
  `SceneVisuals` が縦に引き伸ばして（`top:-9%` / `height:125%`）画面外へ逃がしている。
  **画面当てクイズ型の出題カットだけは例外**で、`SceneVisuals` の `screen` モードが
  拡大もパンもせず原寸比のまま中央のモニターに映す（GUIを切らないため）。
  **認定試験型は `exam` モード**で、上部の映像エリア（206〜1080）にだけ縦125%で収める。
  下半分を答案用紙が占めるので、いつもの拡大だとGUIも風景もドアップになって読めない。
  紙のない行（導入と合格後）では全画面に戻る。
  **相場クイズ型は `market` モード**で、考え方は `exam` と同じ（エリアは 204〜1004）。
  売り場のない行（導入と価格表以降）では全画面に戻る。
  **ウソ発見器型は `lie` モード**で、`market` と同じエリア・同じ定数を共用する。
  供述カードのない行（導入と事実リスト以降）では全画面に戻る
- `type: video` を基本とし、テキスト強調シーンは `type: text` + `backgroundSrc` で背景動画を入れる
- **横長のスクリーンショットは `type: image` + `backgroundSrc`** を使う。背景に動画を敷き、
  画像はカードとして浮かせる（そのまま置くと上下が真っ黒に余る）
- 動画素材は必ず **ミュート**（`muted`）にする
- 動画は **左右にパン**させ続ける（SCALE=1.6、lineIdの偶奇で方向を交互に切り替え）。
  **路線図型（`rail` モード）だけは右→左に固定**し、走行中はパンの周期を詰めて
  景色を流す／停車中はほぼ止める。車体の縦揺れも足す
- 動画の開始位置は **疑似乱数**（lineId + src のハッシュ）で決定する（`startFrom` で明示上書きも可）

#### テンポ・音声

| 設定 | 値 |
|------|----|
| playbackRate | 1.5 |
| pauseAfter（連発パート） | -4 |
| pauseAfter（それ以外） | -3 |
| pauseAfter（最後のセリフ） | **0**（余白を作らずループへ） |

- 連発パートのセリフは「家を建てる。」のように **体言止めの1フレーズ**にする（1秒未満に収める）
- 街頭インタビュー型・コメント返信型のように「振り（質問）→ 受け（回答）」で2行1組に
  する場合は、振りの行を `-4`、受けの行を `-3` にして組の中だけ詰める
- 効果音（SE）は**全セリフに入れる**
- BGMは必ず流す（`defaults.yaml` の `bgm` セクションで設定）。
  連発パートは速い曲、リビールで切り替えて空気を変える

#### 字幕アニメーション

字幕は下からフェードイン・上へフェードアウトするアニメーションをつける（`Subtitle.tsx` 実装済み）。

字幕のアウトラインはほぼ黒の紺（`#070c1a`）で固定する。明るい草原にも暗い洞窟にも乗るため。
また太い袋文字だと「……」が四角い塊に見えるので、`displayText` に三点リーダーを使わない
（`text` 側は音声の間として残してよい）。

字幕は最下部に寄せる。**今後の新フォーマットは `bottomOffset: 60`**
（ティッカー帯を作らないため。設計原則10を参照）。既存14フォーマットは
最下部92pxのティッカー帯の上に逃がすため `bottomOffset: 150` のままにしてある。
`displayText` は **1〜2行に収まる長さ**にする。3行になると画面が重い。

**クイズ$ミリオネア型は例外的に映像を大きく見せる型ではない**（賞金ラダーと選択肢で
画面の上下が埋まる）。それ以外は下記のとおり。

**縦型ショートドラマ型だけは例外**で、標準の `Subtitle` を全行で抑止し、
話者タグ付きの「ドラマ字幕」（`dramaLine`）を HUD 側が出す。
ドラマではセリフを読ませること自体が主役なので、テロップに兼ねさせない。

それ以外のフォーマットではテロップ／プレート／カード／リビール帯が画面テロップを兼ねるので、
**字幕は検索CTAの行だけ**出す。`hidesSubtitle` が抑止する行は、
ウソ発見器型では `lieHook` / `lieCards` / `lieExplain` / `lieRetort` / `lieFlash` /
`lieList` / `lieReveal` / `lieResult`（解説パネルは読ませること自体が本体なので、
字幕と二重にしない。CTAの行では供述カードが畳まれているので字幕が読める）、
相場クイズ型では `mktHook` / `mktItem` / `mktChoices` / `mktExplain` / `mktRetort` /
`mktFlash` / `mktTable` / `mktReveal` / `mktResult`（解説パネルは読ませること自体が
本体なので、字幕と二重にしない。CTAの行では売り場が畳まれているので字幕が読める）、
認定試験型では `examHook` / `examQ` / `examChoices` / `examExplain` / `examRetort` /
`examFlash` / `examCert` / `examReveal` / `examResult`（解説パネルは読ませること自体が
本体なので、字幕と二重にしない。CTAの行では答案用紙が畳まれているので字幕が読める）、
路線図型では `railSign` / `railInfo` / `railRetort` / `railFlash` / `railFareSlam` /
`railReveal` / `railResult`、
参加導線型では `joinScreen` / `joinCard` / `joinRetort` / `joinFlash` / `joinDone` /
`joinPrice` / `joinReveal` / `joinResult`（マイクラUIパネル自体を読ませたいので
パネルが出ている行も抑止する）、
正直CM型では `pvCard` / `pvRetort` / `pvFlash` / `pvPrice` / `pvReveal` / `pvResult`、
クイズ$ミリオネア型では `milHook` / `milQ` / `milChoices` / `milLifeline` /
`milVerdict` / `milRetort` / `milFlash` / `milWin` / `milReveal` / `milResult`、
画面当てクイズ型では `quizHook` / `quizQ` / `quizChoices` / `quizVerdict` /
`quizRetort` / `quizFlash` / `quizReveal` / `quizResult`、
巻き戻し型では `rwLog` / `rwRetort` / `rwFlash` / `rwOrigin` / `rwReveal` / `rwResult`、
リスポーン型では `respMemo` / `respRetort` / `respFlash` / `respDeath` / `respSpawn` /
`respReveal` / `respResult`、
求人票型では `jobTerm` / `jobRetort` / `jobFlash` / `jobBreak` / `jobReveal` / `jobResult`、
街頭インタビュー型では `intvQuestion` / `intvAnswer` / `intvFlash` / `intvWrapUp` /
`intvReveal` / `intvResult`、
コメント返信型では `replyQuestion` / `replyAnswer` / `replyFlash` / `replyClear` /
`replyReveal` / `replyResult`、裁判型では `courtFlash` / `courtCharge` / `courtObjection` /
`courtLower` / `courtJudgment` / `courtReveal` / `courtResult`、報道型では `newsFlash` /
`newsLower` / `newsCorrection` / `newsReveal` / `newsResult` を持つ行。

#### 技術的注意点（レンダリング品質）

- 動画素材には `<OffthreadVideo>` を使う（`<Video>` はレンダリング時にfpsが落ちる）
- 1秒ごとにカットが変わるので、映像のSequenceには `premountFor={fps}` を付ける
  （付けないとデコードが間に合わずカット頭が黒コマになる）
- 映像・HUD・字幕のSequenceは `getLineSpan()`（尺＋正のpause）で囲む。
  `getLineDuration()` だけだと最後の行の間に素の背景が数フレーム覗く
- BGMは `<Sequence durationInFrames={...}>` で囲む（レンダリング時の音声はみ出し防止）
- `Root.tsx` の `calculateTotalFrames()` は必ず `getAdjustedFrames`（÷playbackRate）を適用する
- **PowerShellの `-replace` + `Set-Content` でソースを書き換えないこと。**
  UTF-8の日本語コメントが文字化けし、改行も壊れてコードがコメントに飲まれる。
  必ず Edit ツールで編集する

---

## Claude Code Skillの導入

Claude Codeをより効率的に使うために、専用Skillを導入できます：

```bash
mkdir -p ~/.claude/skills/remotion-video
curl -o ~/.claude/skills/remotion-video/SKILL.md \
  https://raw.githubusercontent.com/nyanko3141592/remotion-voicevox-template/master/SKILL.md
```

Skillを入れると：
- 「紹介動画を作りたい」と言うだけで適切なワークフローを案内
- 英語→カタカナ変換、音声生成の手順を熟知した状態で開始
